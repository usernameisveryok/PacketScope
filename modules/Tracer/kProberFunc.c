
#include <net/sock.h>
#include <linux/sched.h>
#include <linux/uio.h>

struct SkProbe
{
	u32 pid;
	u32 padding32;
	u64 kernelTime;
	// char comm[TASK_COMM_LEN];
	// BPF info above
    u64 FuncID;
    u64 ret;
    u64 family;
	u64 dport;
	u64 lport;
	u32 ipv4__sendaddr;
    u32 ipv4__recvaddr;
    u8 ipv6__sendaddr[16];
    u8 ipv6__recvaddr[16];
};

struct packet_metadata
{
    u64 isPacket;
    u64 timestamp;
    u64 pid;
    u64 FuncID;
    u64 payloadlen;
    u8 payloadHdr[58];
};
BPF_RINGBUF_OUTPUT(events, 512);
BPF_RINGBUF_OUTPUT(SpecEvents, 128);



int ktprobe_ip_rcv_core(struct pt_regs *ctx,struct sk_buff *skb)
{
struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
if(!data){{return 0;}}
//struct sock *sk = skb->sk;
u8 *data_s = (u8 *)(long)skb->data;
u8 *data_end = (u8 *)(long)skb->tail;
data->ret = 0;
data->FuncID=200000;
data->kernelTime = bpf_ktime_get_ns();
data->pid = bpf_get_current_pid_tgid();
/*
if(skb->len<16)
{
    data->family = (long)skb->data;
    data->dport = (long)skb->tail;
    events.ringbuf_submit(data, 0);
    return 0;
    // No complete Eth layer
}
*/
u8 ipver=data_s[0]&0xf0;
if(ipver==64)
{
// ipv4
u32 headerlen=(data_s[0]&0x0f)*4;
u32 nextprotstart=headerlen;
u32 subprot=data_s[9];
if(subprot == 17 || subprot == 6)
{
    // UDP or TCP
    data->family = 4; 
    data->ipv4__sendaddr=(data_s[15] << 24) | (data_s[14] << 16) | (data_s[13] << 8) | data_s[12];
    data->ipv4__recvaddr=(data_s[19] << 24) | (data_s[18] << 16) | (data_s[17] << 8) | data_s[16];
    data->lport=data_s[nextprotstart+1]+data_s[nextprotstart]*256;
    data->dport=data_s[nextprotstart+3]+data_s[nextprotstart+2]*256;
    events.ringbuf_submit(data, 0);
    return 0;
}
}
else if(ipver==96)
{
    u32 headertype=data_s[6];
    if(headertype == 6 || headertype==17)
    // TCP/UDP
    {
    data->family = 6;
    bpf_probe_read(&data->ipv6__sendaddr,16,data_s+8);
    bpf_probe_read(&data->ipv6__recvaddr,16,data_s+24);
    data->lport=data_s[41]+data_s[42]*256;
    data->dport=data_s[43]+data_s[44]*256;
    }
    // ipv6
}
events.ringbuf_submit(data, 0);
return 0;
}

int ktretprobe_ip_rcv_core(struct pt_regs *ctx)
{
struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
if(!data){{return 0;}}
data->FuncID=200000;
data->kernelTime = bpf_ktime_get_ns();
data->pid=bpf_get_current_pid_tgid();
data->ret=1;
events.ringbuf_submit(data, 0);
return 0;
}

int ktprobe_ip6_rcv_core(struct pt_regs *ctx,struct sk_buff *skb)
{
struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
if(!data){{return 0;}}
//struct sock *sk = skb->sk;
u8 *data_s = (u8 *)(long)skb->data;
u8 *data_end = (u8 *)(long)skb->tail;
data->ret = 0;
data->FuncID=200001;
data->kernelTime = bpf_ktime_get_ns();
data->pid = bpf_get_current_pid_tgid();
/*
if(skb->len<16)
{
    data->family = (long)skb->data;
    data->dport = (long)skb->tail;
    events.ringbuf_submit(data, 0);
    return 0;
    // No complete Eth layer
}
*/
u8 ipver=data_s[0]&0xf0;
if(ipver==64)
{
// ipv4
u32 headerlen=(data_s[0]&0x0f)*4;
u32 nextprotstart=headerlen;
u32 subprot=data_s[9];
if(subprot == 17 || subprot == 6)
{
    // UDP or TCP
    data->family = 4; 
    data->ipv4__sendaddr=(data_s[15] << 24) | (data_s[14] << 16) | (data_s[13] << 8) | data_s[12];
    data->ipv4__recvaddr=(data_s[19] << 24) | (data_s[18] << 16) | (data_s[17] << 8) | data_s[16];
    data->lport=data_s[nextprotstart+1]+data_s[nextprotstart]*256;
    data->dport=data_s[nextprotstart+3]+data_s[nextprotstart+2]*256;
    events.ringbuf_submit(data, 0);
    return 0;
}
}
else if(ipver==96)
{
    u32 headertype=data_s[6];
    if(headertype == 6 || headertype==17)
    // TCP/UDP
    {
    data->family = 6;
    bpf_probe_read(&data->ipv6__sendaddr,16,data_s+8);
    bpf_probe_read(&data->ipv6__recvaddr,16,data_s+24);
    data->lport=data_s[41]+data_s[42]*256;
    data->dport=data_s[43]+data_s[44]*256;
    }
    // ipv6
}
events.ringbuf_submit(data, 0);
return 0;
}

int ktretprobe_ip6_rcv_core(struct pt_regs *ctx)
{
struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
if(!data){{return 0;}}
data->FuncID=200001;
data->kernelTime = bpf_ktime_get_ns();
data->pid=bpf_get_current_pid_tgid();
data->ret=1;
events.ringbuf_submit(data, 0);
return 0;
}


int ktprobe_icmp_push_reply(struct pt_regs *ctx, struct sock *sk)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){{return 0;}}
    data->kernelTime = bpf_ktime_get_ns();
    data->pid = bpf_get_current_pid_tgid();
    data->FuncID=200002;
    data->ret=0;
    u16 dport = sk->__sk_common.skc_dport;
    data->dport = (dport >> 8) | ((dport << 8) & 0xff00);
    data->lport = sk->__sk_common.skc_num;
    u32 family = sk->__sk_common.skc_family;
    if (family == AF_INET6)
    {
        data->family = 6;
        bpf_probe_read(&data->ipv6__recvaddr, sizeof(sk->__sk_common.skc_v6_daddr.s6_addr),
                       &sk->__sk_common.skc_v6_daddr.s6_addr);
        bpf_probe_read(&data->ipv6__sendaddr, sizeof(sk->__sk_common.skc_v6_rcv_saddr.s6_addr),
                       &sk->__sk_common.skc_v6_rcv_saddr.s6_addr);
    }
    else
    {
        data->family = 4;
        data->ipv4__recvaddr = sk->__sk_common.skc_daddr;
        data->ipv4__sendaddr = sk->__sk_common.skc_rcv_saddr;
    }
    events.ringbuf_submit(data, 0);
    return 0;
}

int ktretprobe_icmp_push_reply(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){{return 0;}}
    data->FuncID=200002;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}

int ktprobe_raw_sendmsg(struct pt_regs *ctx, struct sock *sk)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){{return 0;}}
    data->kernelTime = bpf_ktime_get_ns();
    data->pid = bpf_get_current_pid_tgid();
    data->FuncID=200003;
    data->ret=0;
    u16 dport = sk->__sk_common.skc_dport;
    data->dport = (dport >> 8) | ((dport << 8) & 0xff00);
    data->lport = sk->__sk_common.skc_num;
    u32 family = sk->__sk_common.skc_family;
    if (family == AF_INET6)
    {
        data->family = 6;
        bpf_probe_read(&data->ipv6__recvaddr, sizeof(sk->__sk_common.skc_v6_daddr.s6_addr),
                       &sk->__sk_common.skc_v6_daddr.s6_addr);
        bpf_probe_read(&data->ipv6__sendaddr, sizeof(sk->__sk_common.skc_v6_rcv_saddr.s6_addr),
                       &sk->__sk_common.skc_v6_rcv_saddr.s6_addr);
    }
    else
    {
        data->family = 4;
        data->ipv4__recvaddr = sk->__sk_common.skc_daddr;
        data->ipv4__sendaddr = sk->__sk_common.skc_rcv_saddr;
    }
    events.ringbuf_submit(data, 0);
    return 0;
}

int ktretprobe_raw_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){{return 0;}}
    data->FuncID=200003;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}

int ktprobe_rawv6_sendmsg(struct pt_regs *ctx, struct sock *sk)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){{return 0;}}
    data->kernelTime = bpf_ktime_get_ns();
    data->pid = bpf_get_current_pid_tgid();
    data->FuncID=200004;
    data->ret=0;
    u16 dport = sk->__sk_common.skc_dport;
    data->dport = (dport >> 8) | ((dport << 8) & 0xff00);
    data->lport = sk->__sk_common.skc_num;
    u32 family = sk->__sk_common.skc_family;
    if (family == AF_INET6)
    {
        data->family = 6;
        bpf_probe_read(&data->ipv6__recvaddr, sizeof(sk->__sk_common.skc_v6_daddr.s6_addr),
                       &sk->__sk_common.skc_v6_daddr.s6_addr);
        bpf_probe_read(&data->ipv6__sendaddr, sizeof(sk->__sk_common.skc_v6_rcv_saddr.s6_addr),
                       &sk->__sk_common.skc_v6_rcv_saddr.s6_addr);
    }
    else
    {
        data->family = 4;
        data->ipv4__recvaddr = sk->__sk_common.skc_daddr;
        data->ipv4__sendaddr = sk->__sk_common.skc_rcv_saddr;
    }
    events.ringbuf_submit(data, 0);
    return 0;
}

int ktretprobe_rawv6_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){{return 0;}}
    data->FuncID=200004;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}

int ktprobe_udp_sendmsg(struct pt_regs *ctx, struct sock *sk)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){{return 0;}}
    data->kernelTime = bpf_ktime_get_ns();
    data->pid = bpf_get_current_pid_tgid();
    data->FuncID=200005;
    data->ret=0;
    u16 dport = sk->__sk_common.skc_dport;
    data->dport = (dport >> 8) | ((dport << 8) & 0xff00);
    data->lport = sk->__sk_common.skc_num;
    u32 family = sk->__sk_common.skc_family;
    if (family == AF_INET6)
    {
        data->family = 6;
        bpf_probe_read(&data->ipv6__recvaddr, sizeof(sk->__sk_common.skc_v6_daddr.s6_addr),
                       &sk->__sk_common.skc_v6_daddr.s6_addr);
        bpf_probe_read(&data->ipv6__sendaddr, sizeof(sk->__sk_common.skc_v6_rcv_saddr.s6_addr),
                       &sk->__sk_common.skc_v6_rcv_saddr.s6_addr);
    }
    else
    {
        data->family = 4;
        data->ipv4__recvaddr = sk->__sk_common.skc_daddr;
        data->ipv4__sendaddr = sk->__sk_common.skc_rcv_saddr;
    }
    events.ringbuf_submit(data, 0);
    return 0;
}

int ktretprobe_udp_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){{return 0;}}
    data->FuncID=200005;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}

int ktprobe_udpv6_sendmsg(struct pt_regs *ctx, struct sock *sk)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){{return 0;}}
    data->kernelTime = bpf_ktime_get_ns();
    data->pid = bpf_get_current_pid_tgid();
    data->FuncID=200006;
    data->ret=0;
    u16 dport = sk->__sk_common.skc_dport;
    data->dport = (dport >> 8) | ((dport << 8) & 0xff00);
    data->lport = sk->__sk_common.skc_num;
    u32 family = sk->__sk_common.skc_family;
    if (family == AF_INET6)
    {
        data->family = 6;
        bpf_probe_read(&data->ipv6__recvaddr, sizeof(sk->__sk_common.skc_v6_daddr.s6_addr),
                       &sk->__sk_common.skc_v6_daddr.s6_addr);
        bpf_probe_read(&data->ipv6__sendaddr, sizeof(sk->__sk_common.skc_v6_rcv_saddr.s6_addr),
                       &sk->__sk_common.skc_v6_rcv_saddr.s6_addr);
    }
    else
    {
        data->family = 4;
        data->ipv4__recvaddr = sk->__sk_common.skc_daddr;
        data->ipv4__sendaddr = sk->__sk_common.skc_rcv_saddr;
    }
    events.ringbuf_submit(data, 0);
    return 0;
}

int ktretprobe_udpv6_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){{return 0;}}
    data->FuncID=200006;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktprobe_tcp_sendmsg(struct pt_regs *ctx, struct sock *sk)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){{return 0;}}
    data->kernelTime = bpf_ktime_get_ns();
    data->pid = bpf_get_current_pid_tgid();
    data->FuncID=200007;
    data->ret=0;
    u16 dport = sk->__sk_common.skc_dport;
    data->dport = (dport >> 8) | ((dport << 8) & 0xff00);
    data->lport = sk->__sk_common.skc_num;
    u32 family = sk->__sk_common.skc_family;
    if (family == AF_INET6)
    {
        data->family = 6;
        bpf_probe_read(&data->ipv6__recvaddr, sizeof(sk->__sk_common.skc_v6_daddr.s6_addr),
                       &sk->__sk_common.skc_v6_daddr.s6_addr);
        bpf_probe_read(&data->ipv6__sendaddr, sizeof(sk->__sk_common.skc_v6_rcv_saddr.s6_addr),
                       &sk->__sk_common.skc_v6_rcv_saddr.s6_addr);
    }
    else
    {
        data->family = 4;
        data->ipv4__recvaddr = sk->__sk_common.skc_daddr;
        data->ipv4__sendaddr = sk->__sk_common.skc_rcv_saddr;
    }
    events.ringbuf_submit(data, 0);
    return 0;
}

int ktretprobe_tcp_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){{return 0;}}
    data->FuncID=200007;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}

int ktprobe_ip_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=300000;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=300000;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktprobe_ipv6_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=300001;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=300001;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktprobe_ip_list_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=300002;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_list_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=300002;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktprobe_ipv6_list_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=300003;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_list_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=300003;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}

int ktprobe___fl6_sock_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=34725;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___fl6_sock_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=34725;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___inet_listen_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35170;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___inet_listen_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35170;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___inet_lookup_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35172;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___inet_lookup_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35172;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip6_local_out(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35230;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip6_local_out(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35230;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip_append_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35232;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip_append_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35232;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip_do_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35234;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip_do_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35234;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip_finish_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35235;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip_finish_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35235;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip_flush_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35237;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip_flush_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35237;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip_local_out(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35238;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip_local_out(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35238;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip_make_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35240;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip_make_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35240;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip_queue_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35242;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip_queue_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35242;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip_rt_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35244;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip_rt_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35244;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip_select_ident(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35246;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip_select_ident(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35246;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ipv4_sk_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35248;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ipv4_sk_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35248;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ipv6_chk_addr_and_flags(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35250;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ipv6_chk_addr_and_flags(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35250;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ipv6_dev_ac_dec(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35252;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ipv6_dev_ac_dec(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35252;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ipv6_dev_ac_inc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35253;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ipv6_dev_ac_inc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35253;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ipv6_dev_get_saddr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35255;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ipv6_dev_get_saddr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35255;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ipv6_dev_mc_dec(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35256;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ipv6_dev_mc_dec(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35256;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ipv6_dev_mc_inc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35258;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ipv6_dev_mc_inc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35258;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ipv6_ifa_notify(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35260;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ipv6_ifa_notify(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35260;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ipv6_sock_ac_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35264;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ipv6_sock_ac_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35264;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ipv6_sock_mc_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35265;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ipv6_sock_mc_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35265;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ipv6_sock_mc_join(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35267;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ipv6_sock_mc_join(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35267;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___lock_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35352;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___lock_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35352;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___lock_sock_fast(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35354;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___lock_sock_fast(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35354;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_fastopen_gen_msk_ackseq(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35465;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_fastopen_gen_msk_ackseq(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35465;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___release_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35812;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___release_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35812;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___scm_recv_common(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35863;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___scm_recv_common(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35863;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___scm_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35865;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___scm_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35865;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sk_backlog_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35892;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sk_backlog_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35892;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sk_dst_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35895;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sk_dst_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35895;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sk_flush_backlog(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35896;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sk_flush_backlog(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35896;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sk_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35897;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sk_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35897;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sk_mem_raise_allocated(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35899;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sk_mem_raise_allocated(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35899;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sk_mem_reclaim(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35901;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sk_mem_reclaim(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35901;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sk_mem_reduce_allocated(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35902;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sk_mem_reduce_allocated(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35902;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sk_mem_schedule(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sk_mem_schedule(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sk_receive_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35906;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sk_receive_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35906;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35910;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35910;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_header_pointer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35912;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_header_pointer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35912;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sock_cmsg_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35916;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sock_cmsg_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35916;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sock_i_ino(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35920;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sock_i_ino(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35920;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sock_queue_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35921;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sock_queue_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35921;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sock_set_timestamps(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35923;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sock_set_timestamps(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35923;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sock_wfree(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35924;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sock_wfree(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35924;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___strp_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35926;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___strp_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35926;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___tcp_cleanup_rbuf(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35949;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___tcp_cleanup_rbuf(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35949;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___tcp_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35951;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___tcp_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35951;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___tcp_md5_do_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35953;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___tcp_md5_do_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35953;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___tcp_md5_do_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35955;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___tcp_md5_do_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35955;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___tcp_sock_set_cork(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35957;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___tcp_sock_set_cork(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35957;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___tcp_sock_set_nodelay(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35958;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___tcp_sock_set_nodelay(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35958;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___tcp_sock_set_quickack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35960;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___tcp_sock_set_quickack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35960;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___tcp_v4_send_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35962;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___tcp_v4_send_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=35962;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___tun_build_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=36274;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___tun_build_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=36274;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___unix_dgram_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=36317;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___unix_dgram_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=36317;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___unix_stream_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=36320;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___unix_stream_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=36320;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___xfrm4_udp_encap_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=36650;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___xfrm4_udp_encap_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=36650;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___xfrm_sk_clone_policy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=36670;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___xfrm_sk_clone_policy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=36670;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ack_update_msk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=36983;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ack_update_msk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=36983;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_addrconf_ifid_ip6tnl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=37499;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_addrconf_ifid_ip6tnl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=37499;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_arp_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=37848;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_arp_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=37848;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_arp_send_dst(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=37850;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_arp_send_dst(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=37850;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_arp_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=37854;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_arp_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=37854;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_arp_xmit_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=37855;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_arp_xmit_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=37855;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_audit_list_rules_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=37974;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_audit_list_rules_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=37974;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_bq_xmit_all(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=38931;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_bq_xmit_all(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=38931;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_cipso_v4_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39298;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_cipso_v4_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39298;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_cipso_v4_optptr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_cipso_v4_optptr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_cipso_v4_req_delattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39307;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_cipso_v4_req_delattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39307;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_cipso_v4_req_setattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39309;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_cipso_v4_req_setattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39309;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_cipso_v4_skbuff_delattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39311;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_cipso_v4_skbuff_delattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39311;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_cipso_v4_skbuff_setattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39313;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_cipso_v4_skbuff_setattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39313;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_cipso_v4_sock_delattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39314;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_cipso_v4_sock_delattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39314;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_cipso_v4_sock_getattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39316;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_cipso_v4_sock_getattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39316;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_cipso_v4_sock_setattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_cipso_v4_sock_setattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_cipso_v4_validate(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39320;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_cipso_v4_validate(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39320;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_convert___skb_to_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39637;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_convert___skb_to_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39637;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_convert_skb_to___skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39645;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_convert_skb_to___skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=39645;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_crypto_skcipher_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40100;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_crypto_skcipher_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40100;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_crypto_skcipher_report_stat(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40101;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_crypto_skcipher_report_stat(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40101;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_current_check_access_socket(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40160;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_current_check_access_socket(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40160;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dcb_getrewr_prio_dscp_mask_map(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40298;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dcb_getrewr_prio_dscp_mask_map(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40298;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dcb_getrewr_prio_pcp_mask_map(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40300;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dcb_getrewr_prio_pcp_mask_map(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40300;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dcb_ieee_getapp_default_prio_mask(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40303;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dcb_ieee_getapp_default_prio_mask(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40303;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dcb_ieee_getapp_dscp_prio_mask_map(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dcb_ieee_getapp_dscp_prio_mask_map(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dcb_ieee_getapp_mask(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40306;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dcb_ieee_getapp_mask(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40306;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dcb_ieee_getapp_prio_dscp_mask_map(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40307;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dcb_ieee_getapp_prio_dscp_mask_map(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40307;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_default_read_sock_done(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40392;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_default_read_sock_done(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40392;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dequeue_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40438;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dequeue_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=40438;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_do_tcp_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=41573;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_do_tcp_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=41573;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_do_tcp_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=41575;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_do_tcp_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=41575;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dst_cache_set_ip4(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=42195;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dst_cache_set_ip4(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=42195;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dst_cache_set_ip6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=42197;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dst_cache_set_ip6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=42197;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dummy_ip6_datagram_recv_ctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=42216;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dummy_ip6_datagram_recv_ctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=42216;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dummy_ipv6_chk_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=42218;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dummy_ipv6_chk_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=42218;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dummy_ipv6_icmp_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=42220;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dummy_ipv6_icmp_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=42220;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dummy_ipv6_recv_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=42222;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dummy_ipv6_recv_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=42222;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_fib_multipath_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=43223;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_fib_multipath_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=43223;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_fl6_free_socklist(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=43403;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_fl6_free_socklist(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=43403;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_get_tcp4_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=44195;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_get_tcp4_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=44195;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_handshake_sk_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=44301;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_handshake_sk_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=44301;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_hook_socket_bind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=44378;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_hook_socket_bind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=44378;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_hook_socket_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=44379;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_hook_socket_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=44379;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_igmp6_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=44924;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_igmp6_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=44924;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_bind_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45006;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_bind_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45006;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_ctl_sock_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45014;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_ctl_sock_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45014;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_recv_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45030;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_recv_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45030;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45032;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45032;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_send_prepare(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45040;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_send_prepare(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45040;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45042;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45042;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_sk_rebuild_header(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45045;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_sk_rebuild_header(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45045;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_sk_reselect_saddr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45046;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_sk_reselect_saddr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45046;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_sk_rx_dst_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45048;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_sk_rx_dst_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45048;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_sk_set_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45050;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_sk_set_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45050;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_sk_state_store(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45052;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_sk_state_store(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45052;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_sock_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45053;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_sock_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45053;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_dst_hoplimit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45806;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_dst_hoplimit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45806;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_find_1stfragopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45808;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_find_1stfragopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45808;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_flowlabel_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45812;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_flowlabel_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45812;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_flowlabel_proc_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45813;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_flowlabel_proc_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45813;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_local_out(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45814;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_local_out(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45814;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_mc_add_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45816;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_mc_add_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45816;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_mc_del1_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45818;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_mc_del1_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45818;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_mc_del_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45819;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_mc_del_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45819;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_mc_find_dev_rtnl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45821;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_mc_find_dev_rtnl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45821;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_mc_hdr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45823;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_mc_hdr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45823;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_mc_leave_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45825;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_mc_leave_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45825;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_mc_msfget(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_mc_msfget(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_mc_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45829;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_mc_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45829;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_mc_source(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45831;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_mc_source(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45831;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_sk_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45837;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_sk_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45837;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_append_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_append_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_build_and_send_pkt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45841;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_build_and_send_pkt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45841;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_copy_metadata(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45845;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_copy_metadata(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45845;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_do_fragment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45847;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_do_fragment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45847;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_do_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45848;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_do_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45848;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45849;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45849;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_finish_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45852;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_finish_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45852;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_finish_output2(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45853;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_finish_output2(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45853;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_flush_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45854;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_flush_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45854;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_frag_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45856;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_frag_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45856;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_frag_next(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45858;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_frag_next(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45858;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_fraglist_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45860;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_fraglist_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45860;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_fraglist_prepare(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45862;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_fraglist_prepare(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45862;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_fragment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45864;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_fragment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45864;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_generic_getfrag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45866;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_generic_getfrag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45866;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_handle_martian_source(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45868;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_handle_martian_source(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45868;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_local_out(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45872;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_local_out(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45872;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_make_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45874;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_make_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45874;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_finish_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45875;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_finish_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45875;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45876;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45876;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_validate_source(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45878;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_validate_source(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45878;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mtu_from_fib_result(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45882;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mtu_from_fib_result(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45882;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_multipath_l3_keys(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45884;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_multipath_l3_keys(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45884;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45885;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45885;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_push_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45887;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_push_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45887;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_queue_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45889;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_queue_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45889;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_reply_glue_bits(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45891;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_reply_glue_bits(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45891;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_route_input_noref(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45893;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_route_input_noref(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45893;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_route_input_rcu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45895;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_route_input_rcu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45895;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_route_input_slow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45896;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_route_input_slow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45896;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_route_me_harder(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45898;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_route_me_harder(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45898;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_route_output_flow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45900;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_route_output_flow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45900;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_route_output_key_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45902;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_route_output_key_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45902;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_route_output_key_hash_rcu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_route_output_key_hash_rcu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_route_use_hint(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45906;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_route_use_hint(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45906;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_rt_bug(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45907;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_rt_bug(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45907;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_rt_do_proc_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45908;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_rt_do_proc_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45908;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_rt_do_proc_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45909;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_rt_do_proc_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45909;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_rt_get_source(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45911;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_rt_get_source(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45911;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_rt_multicast_event(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45914;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_rt_multicast_event(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45914;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_rt_send_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45915;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_rt_send_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45915;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_rt_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45916;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_rt_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45916;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_send_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45920;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_send_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45920;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_send_unicast_reply(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45922;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_send_unicast_reply(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45922;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_setup_cork(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45924;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_setup_cork(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45924;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_skb_dst_mtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45926;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_skb_dst_mtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45926;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipip_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45929;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipip_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45929;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipip_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45930;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipip_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45930;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipip_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45931;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipip_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45931;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_blackhole_route(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45933;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_blackhole_route(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45933;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_confirm_neigh(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45935;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_confirm_neigh(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45935;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_cow_metrics(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45936;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_cow_metrics(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45936;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_default_advmss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45937;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_default_advmss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45937;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_dst_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45938;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_dst_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45938;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_dst_destroy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45939;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_dst_destroy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45939;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_inetpeer_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45940;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_inetpeer_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45940;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_inetpeer_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45941;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_inetpeer_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45941;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_link_failure(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45942;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_link_failure(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45942;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_mib_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45943;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_mib_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45943;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_mib_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45944;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_mib_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45944;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_mtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45945;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_mtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45945;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_negative_advice(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45947;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_negative_advice(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45947;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_neigh_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45948;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_neigh_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45948;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45951;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45951;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_send_dest_unreach(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45952;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_send_dest_unreach(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45952;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_sk_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45954;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_sk_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45954;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_sk_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45955;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_sk_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45955;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45959;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45959;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_ac_destroy_dev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45960;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_ac_destroy_dev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45960;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_add_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45962;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_add_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45962;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_add_dev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45963;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_add_dev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45963;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_chk_acast_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45967;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_chk_acast_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45967;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_chk_acast_addr_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45968;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_chk_acast_addr_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45968;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_chk_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45969;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_chk_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45969;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_chk_addr_and_flags(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45971;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_chk_addr_and_flags(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45971;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_chk_custom_prefix(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45973;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_chk_custom_prefix(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45973;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_chk_home_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45975;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_chk_home_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45975;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_chk_mcast_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45977;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_chk_mcast_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45977;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_chk_prefix(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45979;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_chk_prefix(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45979;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_chk_rpl_srh_loop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45981;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_chk_rpl_srh_loop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45981;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_create_tempaddr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45983;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_create_tempaddr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45983;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_del_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45984;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_del_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45984;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_dev_find(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45986;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_dev_find(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45986;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_dev_get_saddr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45988;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_dev_get_saddr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45988;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_dev_mc_dec(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45990;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_dev_mc_dec(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45990;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_dev_mc_inc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45991;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_dev_mc_inc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45991;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_find_hdr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45998;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_find_hdr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45998;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_find_idev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45999;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_find_idev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=45999;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_find_tlv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46001;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_find_tlv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46001;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_flowlabel_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46003;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_flowlabel_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46003;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_flowlabel_opt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46005;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_flowlabel_opt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46005;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_flowlabel_opt_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46007;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_flowlabel_opt_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46007;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_generate_eui64(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46008;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_generate_eui64(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46008;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_generate_stable_address(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46010;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_generate_stable_address(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46010;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_get_ifaddr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46012;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_get_ifaddr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46012;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_get_lladdr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46014;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_get_lladdr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46014;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_get_saddr_eval(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46016;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_get_saddr_eval(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46016;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_inherit_eui64(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46020;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_inherit_eui64(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46020;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_mc_config(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46024;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_mc_config(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46024;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_mc_dad_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46025;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_mc_dad_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46025;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_mc_destroy_dev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46026;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_mc_destroy_dev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46026;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_mc_down(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46027;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_mc_down(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46027;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_mc_init_dev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46028;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_mc_init_dev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46028;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_mc_remap(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46030;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_mc_remap(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46030;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_mc_unmap(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46031;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_mc_unmap(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46031;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_mc_up(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46032;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_mc_up(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46032;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_proxy_select_ident(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46034;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_proxy_select_ident(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46034;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_raw_deliver(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46036;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_raw_deliver(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46036;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_select_ident(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46038;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_select_ident(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46038;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_skip_exthdr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46040;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_skip_exthdr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46040;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_sock_ac_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46041;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_sock_ac_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46041;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_sock_ac_drop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46043;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_sock_ac_drop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46043;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_sock_ac_join(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46044;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_sock_ac_join(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46044;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_sock_mc_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46045;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_sock_mc_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46045;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_sock_mc_drop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46046;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_sock_mc_drop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46046;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_sock_mc_join(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46047;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_sock_mc_join(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46047;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_sock_mc_join_ssm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46048;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_sock_mc_join_ssm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46048;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_sysctl_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46049;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_sysctl_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46049;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_sysctl_net_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46050;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_sysctl_net_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46050;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_iw_send_thrspy_event(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46206;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_iw_send_thrspy_event(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=46206;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mctp_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47209;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mctp_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47209;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mctp_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47214;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mctp_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47214;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mctp_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47217;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mctp_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47217;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mctp_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47219;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mctp_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47219;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mctp_sk_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47220;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mctp_sk_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47220;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mctp_sk_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47221;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mctp_sk_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47221;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mctp_sk_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47223;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mctp_sk_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47223;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mctp_sk_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47224;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mctp_sk_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47224;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mctp_sk_unhash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47225;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mctp_sk_unhash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47225;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mem_cgroup_sk_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47401;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mem_cgroup_sk_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47401;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mem_cgroup_sk_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mem_cgroup_sk_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mld_send_cr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47608;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mld_send_cr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47608;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mld_send_initial_cr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47609;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mld_send_initial_cr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47609;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mld_send_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47611;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mld_send_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47611;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mld_sendpack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47612;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mld_sendpack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47612;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_established_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47890;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_established_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47890;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_established_options_add_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47891;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_established_options_add_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47891;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_established_options_dss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47893;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_established_options_dss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47893;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_established_options_mp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47894;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_established_options_mp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47894;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_established_options_mp_fail(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47896;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_established_options_mp_fail(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47896;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_established_options_rst(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47897;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_established_options_rst(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47897;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_fastopen_subflow_synack_set_params(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47899;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_fastopen_subflow_synack_set_params(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47899;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_get_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47901;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_get_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47901;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_get_reset_option(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47903;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_get_reset_option(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47903;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_incoming_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47905;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_incoming_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47905;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_parse_option(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47907;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_parse_option(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47907;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_syn_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47909;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_syn_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47909;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_synack_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47911;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_synack_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47911;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_update_rcv_data_fin(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47913;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_update_rcv_data_fin(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47913;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_write_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47915;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_write_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=47915;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ncsi_send_cmd_nl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48077;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ncsi_send_cmd_nl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48077;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ncsi_send_netlink_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48079;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ncsi_send_netlink_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48079;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ncsi_send_netlink_rsp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48081;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ncsi_send_netlink_rsp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48081;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ncsi_send_netlink_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48082;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ncsi_send_netlink_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48082;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ncsi_set_channel_mask_nl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48083;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ncsi_set_channel_mask_nl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48083;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ncsi_set_package_mask_nl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48085;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ncsi_set_package_mask_nl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48085;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ncsi_xmit_cmd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48091;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ncsi_xmit_cmd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48091;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ndisc_alloc_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48132;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ndisc_alloc_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48132;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ndisc_recv_na(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48161;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ndisc_recv_na(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48161;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ndisc_recv_ns(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48162;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ndisc_recv_ns(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48162;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ndisc_recv_rs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48163;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ndisc_recv_rs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48163;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ndisc_send_na(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48167;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ndisc_send_na(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48167;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ndisc_send_ns(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48169;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ndisc_send_ns(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48169;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ndisc_send_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48171;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ndisc_send_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48171;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ndisc_send_rs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48173;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ndisc_send_rs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48173;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ndisc_send_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48175;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ndisc_send_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48175;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ndisc_send_unsol_na(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48176;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ndisc_send_unsol_na(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48176;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_net_ns_get_ownership(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48207;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_net_ns_get_ownership(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48207;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_net_test_get_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48217;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_net_test_get_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48217;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_net_test_phy_loopback_tcp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48224;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_net_test_phy_loopback_tcp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48224;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_net_test_phy_loopback_udp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48225;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_net_test_phy_loopback_udp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48225;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_net_test_phy_loopback_udp_mtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48226;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_net_test_phy_loopback_udp_mtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48226;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netkit_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48292;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netkit_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48292;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlbl_cipsov4_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48295;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlbl_cipsov4_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48295;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlbl_cipsov4_list(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48301;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlbl_cipsov4_list(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48301;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlbl_cipsov4_listall(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48302;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlbl_cipsov4_listall(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48302;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlbl_cipsov4_remove(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlbl_cipsov4_remove(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netns_ip_rt_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48325;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netns_ip_rt_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48325;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_nf_ip_route(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48345;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_nf_ip_route(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=48345;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ping_v6_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=49437;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ping_v6_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=49437;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_pipefs_init_fs_context(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=49464;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_pipefs_init_fs_context(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=49464;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_pskb_may_pull_reason(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50014;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_pskb_may_pull_reason(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50014;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_raw6_icmp_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50207;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_raw6_icmp_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50207;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_rawv6_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50229;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_rawv6_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50229;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_rawv6_init_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50231;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_rawv6_init_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50231;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_rawv6_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50240;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_rawv6_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50240;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_rawv6_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50242;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_rawv6_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50242;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_rawv6_send_hdrinc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50244;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_rawv6_send_hdrinc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50244;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_rawv6_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50246;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_rawv6_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50246;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_release_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50701;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_release_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50701;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_reqsk_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50734;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_reqsk_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50734;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_rtmsg_ifinfo_build_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50942;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_rtmsg_ifinfo_build_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50942;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_rtmsg_ifinfo_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50946;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_rtmsg_ifinfo_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50946;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_rtnetlink_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50958;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_rtnetlink_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=50958;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_rtnl_kfree_skbs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51002;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_rtnl_kfree_skbs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51002;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_rtnl_set_sk_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51048;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_rtnl_set_sk_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51048;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_rtnl_xdp_prog_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51073;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_rtnl_xdp_prog_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51073;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sch_direct_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51166;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sch_direct_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51166;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_seg6_hmac_validate_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51422;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_seg6_hmac_validate_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51422;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_seg6_icmp_srh(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51424;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_seg6_icmp_srh(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51424;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_netlbl_inet_csk_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51450;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_netlbl_inet_csk_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51450;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_netlbl_sctp_sk_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51454;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_netlbl_sctp_sk_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51454;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_netlbl_skbuff_getsid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51459;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_netlbl_skbuff_getsid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51459;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_netlbl_skbuff_setsid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51461;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_netlbl_skbuff_setsid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51461;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_netlbl_sock_genattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51463;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_netlbl_sock_genattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51463;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_netlbl_sock_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51465;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_netlbl_sock_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51465;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_netlbl_socket_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51467;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_netlbl_socket_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51467;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_netlbl_socket_connect_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51468;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_netlbl_socket_connect_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51468;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_netlbl_socket_post_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51470;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_netlbl_socket_post_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51470;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_netlbl_socket_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51472;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_netlbl_socket_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51472;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51981;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51981;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_capable(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51985;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_capable(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51985;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_clear_memalloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51986;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_clear_memalloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51986;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_clone_lock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51988;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_clone_lock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51988;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_common_release(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51989;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_common_release(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51989;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51990;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51990;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_drops_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51991;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_drops_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51991;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_dst_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51992;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_dst_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51992;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_error_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51993;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_error_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51993;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51994;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51994;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_free_unlock_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51995;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_free_unlock_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51995;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_fullsock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51997;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_fullsock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51997;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_get_meminfo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51999;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_get_meminfo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=51999;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52000;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52000;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52001;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52001;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_leave_memory_pressure(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52002;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_leave_memory_pressure(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52002;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_mc_loop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52003;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_mc_loop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52003;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_net_capable(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52004;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_net_capable(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52004;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_ns_capable(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52006;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_ns_capable(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52006;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_page_frag_refill(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52008;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_page_frag_refill(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52008;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_reset_timer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52012;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_reset_timer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52012;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_send_sigurg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52013;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_send_sigurg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52013;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_set_memalloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52014;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_set_memalloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52014;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_set_peek_off(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52016;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_set_peek_off(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52016;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52017;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52017;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_setup_caps(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52018;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_setup_caps(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52018;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_stop_timer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52020;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_stop_timer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52020;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_stop_timer_sync(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52021;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_stop_timer_sync(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52021;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_wait_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52023;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_wait_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52023;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_advance_to_frag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52025;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_advance_to_frag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52025;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_do_copy_data_nocache(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52027;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_do_copy_data_nocache(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52027;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_eth_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52029;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_eth_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52029;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_gso_transport_seglen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52031;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_gso_transport_seglen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52031;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_gso_validate_mac_len(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52033;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_gso_validate_mac_len(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52033;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_gso_validate_network_len(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52035;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_gso_validate_network_len(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52035;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_mac_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52036;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_mac_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52036;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_orphan_partial(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52037;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_orphan_partial(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52037;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_put_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52041;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_put_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52041;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_smack_ip_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52112;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_smack_ip_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52112;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_bind_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52152;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_bind_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52152;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_bindtoindex(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52154;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_bindtoindex(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52154;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_bindtoindex_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52156;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_bindtoindex_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52156;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_cmsg_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52158;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_cmsg_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52158;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_common_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52159;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_common_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52159;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_common_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52160;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_common_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52160;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_common_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52161;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_common_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52161;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_copy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52163;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_copy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52163;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_def_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52166;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_def_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52166;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_def_error_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52167;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_def_error_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52167;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_def_wakeup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52169;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_def_wakeup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52169;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_def_write_space(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52170;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_def_write_space(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52170;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_efree(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52171;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_efree(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52171;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_enable_timestamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52173;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_enable_timestamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52173;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_enable_timestamps(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52174;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_enable_timestamps(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52174;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_gettstamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52178;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_gettstamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52178;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_hash_alloc_elem(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52181;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_hash_alloc_elem(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52181;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_hash_seq_find_next(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52195;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_hash_seq_find_next(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52195;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_hash_update_common(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52201;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_hash_update_common(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52201;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_i_ino(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52202;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_i_ino(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52202;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_i_uid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52204;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_i_uid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52204;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_init_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52206;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_init_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52206;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_init_data_uid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52208;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_init_data_uid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52208;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_inuse_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52209;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_inuse_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52209;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_inuse_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52210;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_inuse_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52210;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_inuse_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52211;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_inuse_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52211;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_ioctl_inout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52213;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_ioctl_inout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52213;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_kfree_s(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52215;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_kfree_s(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52215;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_kmalloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52217;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_kmalloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52217;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_kzfree_s(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52218;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_kzfree_s(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52218;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_map_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52223;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_map_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52223;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_map_destroy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52225;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_map_destroy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52225;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_map_link(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52237;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_map_link(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52237;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_map_remove_links(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52247;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_map_remove_links(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52247;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_map_sk_state_allowed(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52252;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_map_sk_state_allowed(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52252;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_map_unhash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52253;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_map_unhash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52253;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_map_unref(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52255;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_map_unref(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52255;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_map_update_common(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52257;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_map_update_common(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52257;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_no_accept(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52261;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_no_accept(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52261;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_no_bind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52263;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_no_bind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52263;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_no_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52265;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_no_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52265;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_no_getname(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52267;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_no_getname(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52267;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_no_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52268;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_no_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52268;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_no_linger(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52269;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_no_linger(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52269;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_no_listen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52270;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_no_listen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52270;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_no_mmap(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52272;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_no_mmap(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52272;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_no_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52274;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_no_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52274;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_no_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52276;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_no_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52276;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_no_sendmsg_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52278;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_no_sendmsg_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52278;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_no_shutdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52279;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_no_shutdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52279;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_no_socketpair(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_no_socketpair(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_ofree(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52282;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_ofree(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52282;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_omalloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52284;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_omalloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52284;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_pfree(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52285;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_pfree(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52285;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_prot_inuse_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52287;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_prot_inuse_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52287;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52288;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52288;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_queue_rcv_skb_reason(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52290;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_queue_rcv_skb_reason(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52290;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_recv_errqueue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52292;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_recv_errqueue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52292;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_reserve_memory(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52294;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_reserve_memory(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52294;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_reset_flag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52295;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_reset_flag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52295;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_rfree(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52296;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_rfree(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52296;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_set_flag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52297;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_set_flag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52297;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_set_keepalive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52298;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_set_keepalive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52298;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_set_mark(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52300;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_set_mark(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52300;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_set_priority(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52302;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_set_priority(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52302;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_set_rcvbuf(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52303;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_set_rcvbuf(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52303;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_set_reuseaddr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52304;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_set_reuseaddr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52304;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_set_reuseport(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_set_reuseport(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_set_sndtimeo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52307;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_set_sndtimeo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52307;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_set_timestamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52311;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_set_timestamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52311;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_set_timestamping(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52313;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_set_timestamping(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52313;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52314;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52314;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_valbool_flag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52316;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_valbool_flag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52316;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_wmalloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52319;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_wmalloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52319;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sockopt_lock_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52322;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sockopt_lock_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52322;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sockopt_release_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52325;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sockopt_release_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52325;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_strp_read_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52583;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_strp_read_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52583;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_strp_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52585;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_strp_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52585;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_strp_sock_lock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52586;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_strp_sock_lock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52586;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_strp_sock_unlock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52587;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_strp_sock_unlock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52587;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_switch_task_namespaces(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52705;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_switch_task_namespaces(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52705;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp4_proc_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp4_proc_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp4_proc_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52841;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp4_proc_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52841;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_abort(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52844;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_abort(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52844;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_add_backlog(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52846;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_add_backlog(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52846;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_check_oom(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52852;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_check_oom(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52852;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_cleanup_rbuf(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52853;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_cleanup_rbuf(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52853;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_cleanup_ulp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52854;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_cleanup_ulp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52854;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_clear_md5_list(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52855;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_clear_md5_list(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52855;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52858;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52858;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_disconnect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52860;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_disconnect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52860;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_done(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52861;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_done(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52861;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_downgrade_zcopy_pure(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52862;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_downgrade_zcopy_pure(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52862;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_enter_memory_pressure(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52863;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_enter_memory_pressure(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52863;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_filter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52864;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_filter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52864;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_free_fastopen_req(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52866;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_free_fastopen_req(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52866;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_get_info(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52872;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_get_info(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52872;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_get_info_chrono_stats(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52874;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_get_info_chrono_stats(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52874;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_get_timestamping_opt_stats(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52876;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_get_timestamping_opt_stats(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52876;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52877;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52877;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_inbound_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52879;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_inbound_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52879;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_inbound_md5_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52881;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_inbound_md5_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52881;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_init_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52883;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_init_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52883;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_inq_hint(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52884;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_inq_hint(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52884;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52885;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52885;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ld_RTO_revert(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52887;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ld_RTO_revert(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52887;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_leave_memory_pressure(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52888;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_leave_memory_pressure(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52888;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_mark_push(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52890;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_mark_push(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52890;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_md5_do_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52894;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_md5_do_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52894;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_md5_do_del(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52896;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_md5_do_del(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52896;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_md5_do_lookup_exact(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52898;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_md5_do_lookup_exact(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52898;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_md5_key_copy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52902;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_md5_key_copy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52902;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_md5sig_info_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52905;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_md5sig_info_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52905;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_mmap(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52907;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_mmap(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52907;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_peek_len(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52910;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_peek_len(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52910;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_plb_check_rehash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52912;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_plb_check_rehash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52912;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_plb_update_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52914;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_plb_update_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52914;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_plb_update_state_upon_rto(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52915;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_plb_update_state_upon_rto(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52915;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_push(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52919;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_push(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52919;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rate_check_app_limited(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52920;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rate_check_app_limited(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52920;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rate_gen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52922;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rate_gen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52922;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rate_skb_delivered(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52924;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rate_skb_delivered(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52924;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rate_skb_sent(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52926;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rate_skb_sent(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52926;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_read_done(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52928;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_read_done(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52928;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_read_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52930;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_read_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52930;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_read_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52932;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_read_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52932;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_recv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52934;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_recv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52934;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_recv_timestamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52936;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_recv_timestamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52936;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52937;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52937;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_recvmsg_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52939;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_recvmsg_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52939;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_remove_empty_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52942;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_remove_empty_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52942;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_repair_options_est(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52944;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_repair_options_est(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52944;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_req_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52946;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_req_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52946;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_send_mss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52949;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_send_mss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52949;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sendmsg_fastopen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52953;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sendmsg_fastopen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52953;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sendmsg_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52954;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sendmsg_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52954;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_set_rcvlowat(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52958;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_set_rcvlowat(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52958;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_set_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52959;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_set_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52959;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_set_ulp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52961;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_set_ulp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52961;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_set_window_clamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52962;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_set_window_clamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52962;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52963;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52963;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_shutdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52965;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_shutdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52965;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sigpool_hash_skb_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52975;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sigpool_hash_skb_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52975;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sk_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52979;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sk_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52979;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sk_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52982;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sk_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52982;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_skb_entail(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52983;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_skb_entail(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52983;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sock_set_cork(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52984;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sock_set_cork(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52984;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sock_set_keepcnt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52985;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sock_set_keepcnt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52985;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sock_set_keepidle(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52986;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sock_set_keepidle(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52986;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sock_set_keepidle_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52987;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sock_set_keepidle_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52987;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sock_set_keepintvl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52988;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sock_set_keepintvl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52988;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sock_set_nodelay(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52989;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sock_set_nodelay(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52989;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sock_set_quickack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52990;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sock_set_quickack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52990;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sock_set_syncnt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52991;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sock_set_syncnt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52991;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sock_set_user_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52992;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sock_set_user_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52992;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_splice_data_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52994;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_splice_data_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52994;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_splice_eof(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52995;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_splice_eof(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52995;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_splice_read(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52997;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_splice_read(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52997;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_stream_alloc_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52999;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_stream_alloc_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=52999;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_twsk_unique(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53003;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_twsk_unique(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53003;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_update_recv_tstamps(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53007;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_update_recv_tstamps(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53007;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_update_ulp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53009;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_update_ulp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53009;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_ao_sign_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53011;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_ao_sign_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53011;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_conn_request(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53012;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_conn_request(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53012;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53013;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53013;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_destroy_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53014;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_destroy_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53014;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_do_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53015;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_do_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53015;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_early_demux(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53016;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_early_demux(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53016;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53018;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53018;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_fill_cb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53020;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_fill_cb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53020;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_get_syncookie(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53022;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_get_syncookie(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53022;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_init_seq(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53025;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_init_seq(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53025;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_init_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53026;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_init_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53026;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_init_ts_off(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53028;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_init_ts_off(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53028;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_md5_hash_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53034;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_md5_hash_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53034;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_md5_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53036;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_md5_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53036;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_mtu_reduced(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53037;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_mtu_reduced(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53037;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_parse_md5_keys(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53039;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_parse_md5_keys(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53039;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_pre_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53040;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_pre_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53040;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53041;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53041;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_reqsk_destructor(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53042;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_reqsk_destructor(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53042;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_reqsk_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53044;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_reqsk_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53044;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_restore_cb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53045;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_restore_cb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53045;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_route_req(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53047;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_route_req(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53047;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53049;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53049;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_send_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53050;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_send_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53050;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_send_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53052;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_send_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53052;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_send_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53054;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_send_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53054;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_syn_recv_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53056;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_syn_recv_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53056;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_timewait_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53057;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_timewait_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53057;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_ao_calc_key_rsk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53061;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_ao_calc_key_rsk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53061;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_ao_calc_key_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53063;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_ao_calc_key_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53063;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_ao_calc_key_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53065;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_ao_calc_key_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53065;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_ao_hash_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53069;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_ao_hash_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53069;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_ao_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53071;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_ao_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53071;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_ao_lookup_rsk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53073;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_ao_lookup_rsk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53073;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_ao_synack_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53075;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_ao_synack_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53075;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_parse_ao(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53077;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_parse_ao(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53077;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_wmem_free_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53078;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_wmem_free_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53078;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_wmem_schedule(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53080;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_wmem_schedule(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53080;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_write_queue_purge(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53081;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_write_queue_purge(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53081;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_zerocopy_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53083;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_zerocopy_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53083;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tomoyo_socket_bind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53264;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tomoyo_socket_bind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53264;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tomoyo_socket_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53265;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tomoyo_socket_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53265;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tomoyo_socket_listen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53266;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tomoyo_socket_listen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53266;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tomoyo_socket_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53268;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tomoyo_socket_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53268;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tun_build_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tun_build_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tun_net_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53791;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tun_net_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53791;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tun_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tun_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tun_ring_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53802;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tun_ring_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53802;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tun_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53808;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tun_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53808;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tun_sock_write_space(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53821;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tun_sock_write_space(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53821;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tun_xdp_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53830;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tun_xdp_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53830;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_lib_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53979;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_lib_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53979;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_lib_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53980;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_lib_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53980;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udplite4_proc_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53981;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udplite4_proc_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53981;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udplite4_proc_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53982;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udplite4_proc_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53982;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udplite_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53984;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udplite_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53984;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udplite_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53985;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udplite_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53985;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udplite_sk_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53986;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udplite_sk_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53986;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_uevent_net_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53991;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_uevent_net_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=53991;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_unix_dgram_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54043;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_unix_dgram_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54043;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_unix_dgram_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54044;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_unix_dgram_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54044;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_unix_read_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54061;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_unix_read_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54061;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_unix_release_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54064;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_unix_release_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54064;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_unix_seqpacket_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54071;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_unix_seqpacket_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54071;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_unix_seqpacket_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54072;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_unix_seqpacket_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54072;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_unix_sock_destructor(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54077;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_unix_sock_destructor(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54077;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_unix_socketpair(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54079;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_unix_socketpair(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54079;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_unix_stream_read_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54089;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_unix_stream_read_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54089;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_virtio_net_hdr_to_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54686;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_virtio_net_hdr_to_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=54686;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xennet_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55264;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xennet_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55264;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xennet_xdp_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55273;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xennet_xdp_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55273;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xfrm4_gro_udp_encap_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55277;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xfrm4_gro_udp_encap_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55277;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xfrm4_udp_encap_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55283;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xfrm4_udp_encap_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55283;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xfrm6_ipcomp_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55289;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xfrm6_ipcomp_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55289;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xfrm6_ipcomp_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55290;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xfrm6_ipcomp_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55290;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xfrm_sk_policy_insert(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55423;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xfrm_sk_policy_insert(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55423;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xfrm_sk_policy_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55425;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xfrm_sk_policy_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55425;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xp_add_xsk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55479;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xp_add_xsk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55479;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xp_del_xsk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55499;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xp_del_xsk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=55499;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe____pskb_trim(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=63068;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe____pskb_trim(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=63068;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___consume_stateless_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=63591;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___consume_stateless_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=63591;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___copy_skb_header(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=63602;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___copy_skb_header(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=63602;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip6_del_rt_siblings(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64043;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip6_del_rt_siblings(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64043;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip6_route_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64045;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip6_route_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64045;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip6_rt_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64047;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip6_rt_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64047;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip_options_compile(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64049;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip_options_compile(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64049;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip_options_echo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64051;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip_options_echo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64051;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___kfree_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64075;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___kfree_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64075;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___napi_alloc_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64160;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___napi_alloc_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64160;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___napi_kfree_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64163;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___napi_kfree_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64163;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___netdev_alloc_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64171;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___netdev_alloc_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64171;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ping_queue_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64222;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ping_queue_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64222;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___pskb_copy_fclone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64481;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___pskb_copy_fclone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64481;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___pskb_pull_tail(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64483;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___pskb_pull_tail(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64483;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___pskb_trim_head(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64485;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___pskb_trim_head(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64485;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___reuseport_detach_closed_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64558;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___reuseport_detach_closed_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64558;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___reuseport_detach_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64559;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___reuseport_detach_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64559;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_checksum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64634;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_checksum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64634;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_checksum_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64636;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_checksum_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64636;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_checksum_complete_head(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64638;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_checksum_complete_head(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64638;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64640;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64640;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_complete_tx_timestamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64642;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_complete_tx_timestamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64642;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_ext_del(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64646;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_ext_del(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64646;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_ext_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64650;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_ext_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64650;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_gro_checksum_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64651;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_gro_checksum_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64651;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_pad(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64653;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_pad(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64653;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_recv_udp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64655;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_recv_udp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64655;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_send_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64657;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_send_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64657;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_splice_bits(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64659;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_splice_bits(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64659;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_to_sgvec(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64661;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_to_sgvec(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64661;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_tstamp_tx(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64663;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_tstamp_tx(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64663;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_unclone_keeptruesize(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64665;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_unclone_keeptruesize(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64665;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_vlan_pop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64667;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_vlan_pop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64667;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_warn_lro_forwarding(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64669;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_warn_lro_forwarding(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64669;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_zcopy_downgrade_managed(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64670;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_zcopy_downgrade_managed(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64670;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sock_gen_cookie(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64676;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sock_gen_cookie(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64676;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___tcp_ao_do_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64740;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___tcp_ao_do_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64740;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___tcp_push_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64744;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___tcp_push_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64744;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___tcp_retransmit_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64746;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___tcp_retransmit_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64746;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___tcp_select_window(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64748;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___tcp_select_window(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64748;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___tcp_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64750;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___tcp_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64750;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___tcp_transmit_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64752;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___tcp_transmit_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=64752;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___udp4_lib_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65121;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___udp4_lib_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65121;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___udp4_lib_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65123;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___udp4_lib_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65123;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___udp4_lib_mcast_deliver(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65125;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___udp4_lib_mcast_deliver(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65125;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___udp4_lib_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65127;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___udp4_lib_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65127;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___udp_disconnect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65128;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___udp_disconnect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65128;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___udp_enqueue_schedule_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65129;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___udp_enqueue_schedule_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65129;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___virtnet_xdp_xmit_one(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65181;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___virtnet_xdp_xmit_one(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65181;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___xsk_generic_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65275;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___xsk_generic_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65275;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___xsk_map_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65278;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___xsk_map_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65278;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___xsk_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65280;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___xsk_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65280;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___xsk_rcv_zc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65282;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___xsk_rcv_zc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65282;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___xsk_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65283;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___xsk_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65283;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe__ip6mr_fill_mroute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65317;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe__ip6mr_fill_mroute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65317;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe__ipmr_fill_mroute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe__ipmr_fill_mroute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=65318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_add_recvbuf_big(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66314;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_add_recvbuf_big(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66314;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_alloc_skb_for_msg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66543;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_alloc_skb_for_msg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66543;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_apparmor_ip_postroute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66775;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_apparmor_ip_postroute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66775;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_apparmor_sk_clone_security(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66807;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_apparmor_sk_clone_security(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66807;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_apparmor_sk_free_security(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66808;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_apparmor_sk_free_security(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66808;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_apparmor_sock_graft(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66810;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_apparmor_sock_graft(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66810;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_apparmor_socket_accept(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66811;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_apparmor_socket_accept(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66811;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_apparmor_socket_bind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66813;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_apparmor_socket_bind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66813;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_apparmor_socket_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66814;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_apparmor_socket_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66814;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_apparmor_socket_getpeersec_stream(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66819;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_apparmor_socket_getpeersec_stream(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66819;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_apparmor_socket_getsockname(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66820;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_apparmor_socket_getsockname(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66820;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_apparmor_socket_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66821;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_apparmor_socket_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66821;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_apparmor_socket_listen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66822;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_apparmor_socket_listen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66822;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_apparmor_socket_post_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66824;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_apparmor_socket_post_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66824;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_apparmor_socket_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66828;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_apparmor_socket_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66828;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_apparmor_socket_shutdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66829;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_apparmor_socket_shutdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66829;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_apparmor_socket_sock_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66830;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_apparmor_socket_sock_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66830;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_apparmor_unix_may_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66840;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_apparmor_unix_may_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=66840;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_audit_log_common_recv_msg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=67204;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_audit_log_common_recv_msg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=67204;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_audit_log_task_context(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=67239;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_audit_log_task_context(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=67239;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_audit_log_task_info(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=67240;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_audit_log_task_info(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=67240;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_audit_send_reply(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=67262;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_audit_send_reply(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=67262;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_blackhole_netdev_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=67418;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_blackhole_netdev_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=67418;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_build_skb_around(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=68140;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_build_skb_around(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=68140;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_crypto_akcipher_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=69387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_crypto_akcipher_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=69387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_crypto_akcipher_report_stat(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=69388;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_crypto_akcipher_report_stat(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=69388;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_crypto_lskcipher_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=69453;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_crypto_lskcipher_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=69453;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_crypto_lskcipher_report_stat(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=69454;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_crypto_lskcipher_report_stat(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=69454;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dev_mc_sync_multiple(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=69785;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dev_mc_sync_multiple(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=69785;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dev_uc_sync_multiple(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=69828;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dev_uc_sync_multiple(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=69828;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_features_send_reply(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=72050;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_features_send_reply(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=72050;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_fib_select_multipath(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=72140;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_fib_select_multipath(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=72140;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_free_old_xmit_skbs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=72432;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_free_old_xmit_skbs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=72432;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmp6_dst_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=73815;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmp6_dst_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=73815;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet6_csk_addr2sockaddr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74001;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet6_csk_addr2sockaddr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74001;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet6_csk_route_req(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74003;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet6_csk_route_req(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74003;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet6_csk_route_socket(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74005;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet6_csk_route_socket(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74005;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet6_csk_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74007;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet6_csk_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74007;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet6_csk_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74009;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet6_csk_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74009;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet6_lookup_run_sk_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74026;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet6_lookup_run_sk_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74026;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_twsk_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74037;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_twsk_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74037;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_twsk_hashdance_schedule(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74044;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_twsk_hashdance_schedule(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74044;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_blackhole_route(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74716;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_blackhole_route(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74716;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_confirm_neigh(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74717;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_confirm_neigh(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74717;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_create_rt_rcu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74719;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_create_rt_rcu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74719;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_default_advmss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74720;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_default_advmss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74720;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_del_cached_rt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74722;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_del_cached_rt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74722;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_del_rt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74724;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_del_rt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74724;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_dst_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74726;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_dst_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74726;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_dst_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74727;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_dst_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74727;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_dst_destroy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74728;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_dst_destroy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74728;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_dst_ifdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74731;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_dst_ifdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74731;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_dst_neigh_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74732;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_dst_neigh_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74732;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_frag_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_frag_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_frag_reasm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74737;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_frag_reasm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74737;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_hold_safe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74739;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_hold_safe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74739;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_input(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74740;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_input(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74740;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_input_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74741;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_input_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74741;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_ins_rt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74743;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_ins_rt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74743;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_link_failure(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74744;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_link_failure(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74744;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_list_rcv_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74746;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_list_rcv_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74746;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_mc_input(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74747;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_mc_input(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74747;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_mr_forward(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74750;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_mr_forward(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74750;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_mr_input(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74752;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_mr_input(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74752;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_mroute_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74754;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_mroute_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74754;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_mroute_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74756;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_mroute_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74756;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_mtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74757;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_mtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74757;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_mtu_from_fib6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74759;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_mtu_from_fib6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74759;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_multipath_l3_keys(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74761;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_multipath_l3_keys(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74761;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_negative_advice(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74762;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_negative_advice(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74762;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_neigh_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74764;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_neigh_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74764;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_pkt_discard(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74765;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_pkt_discard(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74765;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_pkt_discard_out(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74766;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_pkt_discard_out(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74766;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_pkt_drop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74768;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_pkt_drop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74768;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_pkt_prohibit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_pkt_prohibit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74769;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_pkt_prohibit_out(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74770;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_pkt_prohibit_out(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74770;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_pol_route(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74772;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_pol_route(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74772;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_pol_route_input(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74773;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_pol_route_input(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74773;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_pol_route_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74774;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_pol_route_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74774;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_pol_route_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74775;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_pol_route_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74775;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_protocol_deliver_rcu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74777;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_protocol_deliver_rcu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74777;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_rcv_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74780;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_rcv_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74780;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_rcv_finish_core(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74782;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_rcv_finish_core(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74782;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74784;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74784;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_redirect_nh_match(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74786;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_redirect_nh_match(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74786;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_redirect_no_header(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74788;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_redirect_no_header(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74788;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_route_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74790;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_route_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74790;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_route_check_nh(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74792;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_route_check_nh(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74792;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_route_del(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74795;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_route_del(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74795;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_route_info_append(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74798;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_route_info_append(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74798;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_route_info_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_route_info_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_route_input(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74803;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_route_input(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74803;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_route_input_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74805;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_route_input_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74805;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_route_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74807;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_route_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74807;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_route_multipath_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74808;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_route_multipath_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74808;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_route_multipath_del(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74809;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_route_multipath_del(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74809;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_route_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74810;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_route_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74810;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_route_net_exit_late(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74811;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_route_net_exit_late(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74811;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_route_net_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74812;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_route_net_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74812;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_route_net_init_late(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74813;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_route_net_init_late(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74813;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_route_output_flags(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74815;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_route_output_flags(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74815;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_route_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74817;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_route_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74817;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_rt_cache_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74819;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_rt_cache_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74819;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_rt_copy_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74821;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_rt_copy_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74821;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_rt_get_dev_rcu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74823;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_rt_get_dev_rcu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74823;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_rt_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74824;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_rt_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74824;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_sk_dst_store_flow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74826;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_sk_dst_store_flow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74826;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_sk_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_sk_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_sk_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74829;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_sk_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74829;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_sublist_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74831;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_sublist_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74831;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74834;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74834;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_validate_gw(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74836;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_validate_gw(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74836;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6frag_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74838;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6frag_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74838;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_cache_find_any(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74843;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_cache_find_any(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74843;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_cache_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74846;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_cache_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74846;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_cache_unresolved(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74848;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_cache_unresolved(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74848;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_compat_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74849;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_compat_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74849;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_destroy_unres(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74851;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_destroy_unres(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74851;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_dump(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74853;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_dump(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74853;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_fib_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74855;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_fib_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74855;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_fill_mroute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74857;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_fill_mroute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74857;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_forward2(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74859;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_forward2(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74859;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_forward2_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74860;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_forward2_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74860;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_get_route(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74862;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_get_route(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74862;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74865;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74865;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_mfc_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74867;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_mfc_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74867;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_mfc_delete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74869;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_mfc_delete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74869;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_mr_table_iter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74871;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_mr_table_iter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74871;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74872;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74872;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_net_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74875;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_net_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74875;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_new_table_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74877;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_new_table_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74877;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_rtm_dumproute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74878;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_rtm_dumproute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74878;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_rtm_getroute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74879;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_rtm_getroute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74879;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_rule_action(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74880;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_rule_action(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74880;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_rule_compare(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74881;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_rule_compare(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74881;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_rule_configure(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74882;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_rule_configure(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74882;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_rule_default(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74883;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_rule_default(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74883;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_rule_fill(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74884;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_rule_fill(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74884;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_rule_match(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74886;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_rule_match(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74886;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_rules_dump(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74887;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_rules_dump(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74887;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_rules_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74888;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_rules_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74888;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_seq_read(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74889;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_seq_read(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74889;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_sk_done(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74890;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_sk_done(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74890;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6mr_update_thresholds(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74892;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6mr_update_thresholds(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74892;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_call_ra_chain(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74896;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_call_ra_chain(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74896;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_encap(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74898;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_encap(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74898;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_fib_check_default(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74900;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_fib_check_default(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74900;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_forward_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74901;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_forward_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74901;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_local_deliver(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_local_deliver(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_local_deliver_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74905;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_local_deliver_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74905;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mr_forward(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74907;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mr_forward(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74907;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mr_input(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74909;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mr_input(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74909;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mroute_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74910;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mroute_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74910;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mroute_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74911;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mroute_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74911;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_options_build(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74913;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_options_build(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74913;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_options_compile(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74915;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_options_compile(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74915;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_options_fragment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74916;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_options_fragment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74916;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_options_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74918;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_options_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74918;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_options_rcv_srr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74919;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_options_rcv_srr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74919;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_protocol_deliver_rcu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74923;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_protocol_deliver_rcu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74923;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_rcv_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74927;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_rcv_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74927;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_rcv_finish_core(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74929;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_rcv_finish_core(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74929;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_rcv_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74931;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_rcv_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74931;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_sublist_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74932;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_sublist_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74932;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_cache_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74980;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_cache_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74980;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_cache_resolve(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74982;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_cache_resolve(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74982;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_cache_unresolved(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74984;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_cache_unresolved(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74984;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_compat_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74985;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_compat_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74985;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_destroy_unres(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74987;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_destroy_unres(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74987;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_do_expire_process(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74990;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_do_expire_process(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74990;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_dump(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74991;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_dump(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74991;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_fib_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74994;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_fib_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74994;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_fill_mroute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74996;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_fill_mroute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74996;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_fill_table(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74998;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_fill_table(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=74998;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_fill_vif(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75000;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_fill_vif(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75000;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_forward_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75001;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_forward_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75001;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_get_route(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75003;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_get_route(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75003;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_init_vif_indev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75006;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_init_vif_indev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75006;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75007;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75007;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_mfc_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75009;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_mfc_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75009;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_mfc_delete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75011;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_mfc_delete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75011;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_mr_table_iter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75014;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_mr_table_iter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75014;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75015;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75015;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_net_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75017;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_net_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75017;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_new_table(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75019;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_new_table(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75019;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_new_table_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75020;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_new_table_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75020;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_new_tunnel(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75022;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_new_tunnel(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75022;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_queue_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75024;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_queue_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75024;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_rt_fib_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75026;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_rt_fib_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75026;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_rtm_dumplink(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75027;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_rtm_dumplink(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75027;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_rtm_dumproute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75028;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_rtm_dumproute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75028;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_rtm_getroute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75029;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_rtm_getroute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75029;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_rtm_route(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75030;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_rtm_route(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75030;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_rtm_valid_getroute_req(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75031;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_rtm_valid_getroute_req(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75031;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_rule_action(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75032;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_rule_action(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75032;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_rule_compare(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75033;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_rule_compare(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75033;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_rule_configure(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75034;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_rule_configure(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75034;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_rule_default(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75035;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_rule_default(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75035;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_rule_fill(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75036;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_rule_fill(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75036;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_rule_match(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75037;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_rule_match(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75037;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_rules_dump(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75038;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_rules_dump(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75038;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_rules_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75039;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_rules_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75039;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_seq_read(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75040;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_seq_read(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75040;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_sk_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75041;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_sk_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75041;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipmr_update_thresholds(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75042;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipmr_update_thresholds(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75042;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_frag_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75050;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_frag_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75050;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_frags_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75051;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_frags_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75051;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_frags_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75052;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_frags_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75052;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_frags_pre_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75053;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_frags_pre_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75053;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_inetpeer_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75054;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_inetpeer_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75054;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_inetpeer_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75055;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_inetpeer_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75055;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_is_mld(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75057;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_is_mld(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75057;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_proc_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75061;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_proc_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75061;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_proc_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75062;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_proc_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75062;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_route_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75065;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_route_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75065;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_route_sysctl_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75067;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_route_sysctl_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75067;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_route_sysctl_table_size(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75069;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_route_sysctl_table_size(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75069;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_kauditd_hold_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75406;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_kauditd_hold_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75406;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_kauditd_rehold_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75407;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_kauditd_rehold_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75407;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_kauditd_retry_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75408;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_kauditd_retry_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75408;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_kauditd_send_multicast_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75409;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_kauditd_send_multicast_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75409;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_kauditd_send_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75411;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_kauditd_send_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75411;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_kfree_skb_list_reason(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75509;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_kfree_skb_list_reason(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75509;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_kfree_skb_partial(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75511;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_kfree_skb_partial(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75511;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_kfree_skb_reason(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75512;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_kfree_skb_reason(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=75512;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_loopback_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=76049;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_loopback_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=76049;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_allow_join_id0(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77066;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_allow_join_id0(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77066;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_close_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77068;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_close_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77068;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_free_local_addr_list(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77074;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_free_local_addr_list(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77074;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_get_add_addr_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77076;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_get_add_addr_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77076;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_get_pm_type(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77077;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_get_pm_type(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77077;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_get_scheduler(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77079;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_get_scheduler(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77079;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_is_checksum_enabled(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77081;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_is_checksum_enabled(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77081;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_is_enabled(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77082;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_is_enabled(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77082;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_join_entry_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77085;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_join_entry_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77085;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77086;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77086;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_net_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77087;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_net_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77087;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_announce_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77088;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_announce_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77088;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_remove_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77089;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_remove_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77089;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_subflow_create_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77090;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_subflow_create_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77090;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_subflow_destroy_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77091;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_subflow_destroy_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77091;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_set_scheduler(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77093;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_set_scheduler(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77093;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_stale_loss_cnt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77094;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_stale_loss_cnt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77094;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_token_join_cookie_init_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77096;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_token_join_cookie_init_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77096;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_userspace_pm_append_new_local_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77098;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_userspace_pm_append_new_local_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77098;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_userspace_pm_delete_local_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77100;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_userspace_pm_delete_local_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77100;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_userspace_pm_get_flags_and_ifindex_by_id(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77102;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_userspace_pm_get_flags_and_ifindex_by_id(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77102;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_userspace_pm_get_local_id(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77104;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_userspace_pm_get_local_id(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77104;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_userspace_pm_is_backup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77106;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_userspace_pm_is_backup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77106;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_userspace_pm_set_flags(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77108;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_userspace_pm_set_flags(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77108;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mroute6_is_socket(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77136;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mroute6_is_socket(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77136;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mrtsock_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77142;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mrtsock_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77142;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_napi_consume_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77200;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_napi_consume_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77200;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_napi_reuse_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77215;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_napi_reuse_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77215;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_napi_skb_cache_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77218;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_napi_skb_cache_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77218;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_napi_skb_free_stolen_head(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77219;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_napi_skb_free_stolen_head(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77219;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_net_dm_packet_trace_kfree_skb_hit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77343;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_net_dm_packet_trace_kfree_skb_hit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77343;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_nh_create_ipv4(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77516;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_nh_create_ipv4(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77516;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_nh_create_ipv6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77517;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_nh_create_ipv6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77517;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_packet_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77866;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_packet_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77866;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_packet_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77888;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_packet_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77888;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_packet_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77890;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_packet_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77890;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_packet_sendmsg_spkt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77891;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_packet_sendmsg_spkt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77891;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_packet_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77898;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_packet_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77898;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_packet_sock_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77900;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_packet_sock_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77900;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_packet_sock_flag_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77902;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_packet_sock_flag_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77902;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_packet_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_packet_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_page_to_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77926;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_page_to_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=77926;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ping_init_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=78756;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ping_init_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=78756;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ping_queue_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=78760;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ping_queue_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=78760;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ping_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=78762;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ping_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=78762;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ping_v4_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=78770;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ping_v4_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=78770;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_pskb_carve_inside_header(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=79404;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_pskb_carve_inside_header(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=79404;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_pskb_carve_inside_nonlinear(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=79406;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_pskb_carve_inside_nonlinear(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=79406;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_pskb_expand_head(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=79408;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_pskb_expand_head(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=79408;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_pskb_extract(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=79410;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_pskb_extract(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=79410;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_pskb_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=79412;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_pskb_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=79412;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_pskb_trim_rcsum_slow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=79413;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_pskb_trim_rcsum_slow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=79413;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_reg_vif_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80108;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_reg_vif_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80108;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_reuseport_add_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80498;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_reuseport_add_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80498;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_reuseport_detach_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80504;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_reuseport_detach_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80504;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_reuseport_migrate_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80510;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_reuseport_migrate_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80510;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_reuseport_select_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80514;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_reuseport_select_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80514;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_reuseport_select_sock_by_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80516;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_reuseport_select_sock_by_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80516;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_reuseport_stop_listen_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80517;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_reuseport_stop_listen_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80517;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_rt6_disable_ip(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80801;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_rt6_disable_ip(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80801;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_rt6_multipath_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_rt6_multipath_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_rt6_multipath_rebalance(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80829;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_rt6_multipath_rebalance(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80829;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_rtm_to_ipmr_mfcc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80924;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_rtm_to_ipmr_mfcc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=80924;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_inet_csk_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_inet_csk_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_mptcp_add_subflow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81484;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_mptcp_add_subflow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81484;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_netlink_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81500;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_netlink_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81500;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_sctp_sk_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81558;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_sctp_sk_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81558;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_sk_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81594;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_sk_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81594;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_sk_classify_flow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81596;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_sk_classify_flow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81596;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_sk_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81597;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_sk_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81597;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_sk_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81598;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_sk_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81598;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_skb_classify_flow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81600;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_skb_classify_flow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81600;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_sock_graft(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81601;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_sock_graft(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81601;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_sock_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81602;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_sock_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81602;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_socket_accept(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81603;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_socket_accept(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81603;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_socket_bind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81604;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_socket_bind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81604;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_socket_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81605;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_socket_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81605;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_socket_getpeersec_stream(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81610;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_socket_getpeersec_stream(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81610;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_socket_getsockname(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81611;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_socket_getsockname(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81611;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_socket_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81612;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_socket_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81612;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_socket_listen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81613;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_socket_listen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81613;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_socket_post_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81614;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_socket_post_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81614;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_socket_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81617;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_socket_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81617;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_socket_shutdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81618;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_socket_shutdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81618;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_socket_socketpair(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81619;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_socket_socketpair(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81619;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_security_unix_may_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81660;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_security_unix_may_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81660;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sendmsg_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81745;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sendmsg_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81745;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sendmsg_unlocked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81746;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sendmsg_unlocked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=81746;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_forced_mem_schedule(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82272;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_forced_mem_schedule(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82272;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_abort_seq_read(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82274;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_abort_seq_read(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82274;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_add_rx_frag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82276;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_add_rx_frag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82276;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_append(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82278;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_append(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82278;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_append_pagefrags(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82280;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_append_pagefrags(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82280;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_attempt_defer_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_attempt_defer_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_checksum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82283;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_checksum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82283;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_checksum_setup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82285;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_checksum_setup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82285;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_checksum_setup_ip(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82287;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_checksum_setup_ip(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82287;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_checksum_setup_ipv6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82288;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_checksum_setup_ipv6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82288;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_checksum_trimmed(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82290;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_checksum_trimmed(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82290;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82292;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82292;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_clone_fraglist(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82293;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_clone_fraglist(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82293;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_clone_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82294;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_clone_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82294;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_coalesce_rx_frag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82296;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_coalesce_rx_frag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82296;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_complete_tx_timestamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82298;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_complete_tx_timestamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82298;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_complete_wifi_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82300;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_complete_wifi_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82300;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_condense(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82301;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_condense(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82301;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_consume_udp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82303;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_consume_udp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82303;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_copy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_copy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_copy_and_csum_bits(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82307;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_copy_and_csum_bits(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82307;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_copy_and_csum_dev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82309;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_copy_and_csum_dev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82309;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_copy_bits(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82311;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_copy_bits(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82311;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_copy_expand(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82313;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_copy_expand(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82313;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_copy_header(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82314;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_copy_header(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82314;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_copy_to_linear_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82316;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_copy_to_linear_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82316;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_copy_ubufs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_copy_ubufs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_cow_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82320;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_cow_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82320;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_dump(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82325;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_dump(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82325;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_ensure_writable(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82327;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_ensure_writable(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82327;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_ensure_writable_head_tail(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82328;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_ensure_writable_head_tail(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82328;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_eth_pop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82331;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_eth_pop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82331;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_eth_push(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82333;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_eth_push(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82333;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_expand_head(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82335;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_expand_head(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82335;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_ext_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82337;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_ext_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82337;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_ext_del(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_ext_del(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_find_text(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82342;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_find_text(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82342;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82346;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82346;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_headers_offset_update(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82348;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_headers_offset_update(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82348;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_metadata_dst_cmp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82351;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_metadata_dst_cmp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82351;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_morph(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82353;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_morph(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82353;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_mpls_dec_ttl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82354;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_mpls_dec_ttl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82354;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_mpls_pop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82356;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_mpls_pop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82356;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_mpls_push(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82358;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_mpls_push(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82358;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_mpls_update_lse(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82360;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_mpls_update_lse(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82360;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_panic(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82362;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_panic(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82362;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_partial_csum_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82364;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_partial_csum_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82364;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_prepare_seq_read(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82366;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_prepare_seq_read(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82366;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_pull(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82368;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_pull(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82368;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_pull_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82370;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_pull_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82370;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_pull_rcsum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82371;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_pull_rcsum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82371;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_push(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82372;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_push(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82372;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_queue_head(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82375;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_queue_head(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82375;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_realloc_headroom(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82381;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_realloc_headroom(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82381;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_reorder_vlan_header(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_reorder_vlan_header(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_scrub_packet(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82389;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_scrub_packet(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82389;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_segment_list(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82393;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_segment_list(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82393;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_send_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82395;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_send_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82395;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_send_sock_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82396;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_send_sock_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82396;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_seq_read(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82398;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_seq_read(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82398;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_shift(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82400;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_shift(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82400;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_splice_bits(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_splice_bits(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_splice_from_iter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82404;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_splice_from_iter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82404;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_split(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82406;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_split(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82406;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_still_in_host_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82408;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_still_in_host_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82408;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_store_bits(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82410;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_store_bits(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82410;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_to_sgvec(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82412;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_to_sgvec(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82412;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_to_sgvec_nomark(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82413;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_to_sgvec_nomark(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82413;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_trim(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82415;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_trim(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82415;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_try_coalesce(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82417;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_try_coalesce(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82417;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_tstamp_tx(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82423;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_tstamp_tx(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82423;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_tx_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82424;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_tx_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82424;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_vlan_pop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82427;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_vlan_pop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82427;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_vlan_push(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82429;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_vlan_push(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82429;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_vlan_untag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82430;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_vlan_untag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82430;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_zerocopy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82433;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_zerocopy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82433;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_zerocopy_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82435;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_zerocopy_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82435;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_zerocopy_headlen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82437;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_zerocopy_headlen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82437;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_zerocopy_iter_stream(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82439;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_zerocopy_iter_stream(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82439;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_smack_inet_csk_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82503;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_smack_inet_csk_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82503;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_smack_sk_alloc_security(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82584;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_smack_sk_alloc_security(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82584;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_smack_sk_clone_security(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82585;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_smack_sk_clone_security(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82585;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_smack_sock_graft(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82586;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_smack_sock_graft(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82586;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_smack_socket_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82588;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_smack_socket_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82588;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_smack_socket_getpeersec_dgram(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82589;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_smack_socket_getpeersec_dgram(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82589;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_smack_socket_getpeersec_stream(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82590;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_smack_socket_getpeersec_stream(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82590;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_smack_socket_post_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82591;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_smack_socket_post_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82591;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_smack_socket_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82592;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_smack_socket_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82592;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_smack_socket_sock_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82593;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_smack_socket_sock_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82593;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_smack_socket_socketpair(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82594;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_smack_socket_socketpair(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82594;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_smack_unix_may_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82609;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_smack_unix_may_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82609;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_smk_ipv4_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82648;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_smk_ipv4_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82648;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_smk_skb_to_addr_ipv6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82654;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_smk_skb_to_addr_ipv6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82654;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_dequeue_err_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82706;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_dequeue_err_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82706;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_diag_bind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82707;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_diag_bind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82707;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_diag_broadcast_destroy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82708;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_diag_broadcast_destroy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82708;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_diag_check_cookie(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82711;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_diag_check_cookie(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82711;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_diag_destroy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82712;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_diag_destroy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82712;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_diag_put_filterinfo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82715;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_diag_put_filterinfo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82715;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_diag_put_meminfo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82717;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_diag_put_meminfo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82717;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_diag_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82718;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_diag_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82718;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_diag_rcv_msg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82719;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_diag_rcv_msg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82719;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_diag_save_cookie(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82725;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_diag_save_cookie(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82725;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_queue_err_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82729;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_queue_err_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82729;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_rmem_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82730;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_rmem_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82730;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sockopt_alloc_buf(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82734;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sockopt_alloc_buf(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82734;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82861;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=82861;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcf_dev_queue_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83333;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcf_dev_queue_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83333;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_adjust_pcount(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83362;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_adjust_pcount(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83362;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_add_cmd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83364;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_add_cmd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83364;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_cache_traffic_keys(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83368;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_cache_traffic_keys(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83368;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_calc_key_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83370;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_calc_key_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83370;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_connect_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83375;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_connect_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83375;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_copy_all_matching(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83377;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_copy_all_matching(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83377;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_del_cmd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83380;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_del_cmd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83380;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_destroy_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83382;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_destroy_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83382;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_do_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83384;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_do_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83384;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_established(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83385;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_established(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83385;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_finish_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83388;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_finish_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83388;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_get_mkts(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83390;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_get_mkts(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83390;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_get_repair(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_get_repair(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_get_sock_info(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83392;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_get_sock_info(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83392;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_hash_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83398;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_hash_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83398;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_ignore_icmp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83400;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_ignore_icmp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83400;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_inbound_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_inbound_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_info_cmd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83403;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_info_cmd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83403;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_prepare_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83410;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_prepare_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83410;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_set_repair(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83412;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_set_repair(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83412;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_syncookie(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83414;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_syncookie(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83414;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_time_wait(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83416;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_time_wait(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83416;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_transmit_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83418;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_transmit_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83418;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ao_verify_ipv6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83420;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ao_verify_ipv6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83420;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_chrono_start(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83435;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_chrono_start(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83435;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_chrono_stop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83436;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_chrono_stop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83436;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_clone_payload(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83438;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_clone_payload(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83438;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83439;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83439;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_connect_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83440;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_connect_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83440;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_connect_queue_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83441;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_connect_queue_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83441;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_current_mss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83443;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_current_mss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83443;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_cwnd_restart(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83445;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_cwnd_restart(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83445;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_cwnd_validate(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83447;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_cwnd_validate(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83447;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_delack_max(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83449;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_delack_max(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83449;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_eat_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83450;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_eat_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83450;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_established_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83452;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_established_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83452;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_event_new_data_sent(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83453;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_event_new_data_sent(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83453;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_fragment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83455;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_fragment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83455;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_fragment_tstamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83457;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_fragment_tstamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83457;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_inbound_ao_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83459;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_inbound_ao_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83459;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_init_tso_segs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83461;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_init_tso_segs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83461;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_make_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83463;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_make_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83463;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_msg_wait_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83465;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_msg_wait_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83465;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_mss_to_mtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83467;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_mss_to_mtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83467;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_mstamp_refresh(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83468;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_mstamp_refresh(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83468;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_mtu_probe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83469;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_mtu_probe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83469;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_mtu_to_mss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83471;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_mtu_to_mss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83471;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_mtup_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83472;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_mtup_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83472;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_options_write(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83474;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_options_write(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83474;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_parse_ao(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83477;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_parse_ao(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83477;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_push_one(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83479;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_push_one(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83479;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_retrans_try_collapse(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83482;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_retrans_try_collapse(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83482;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_retransmit_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83483;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_retransmit_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83483;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rtx_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83485;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rtx_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83485;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_schedule_loss_probe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83487;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_schedule_loss_probe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83487;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_select_initial_window(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83489;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_select_initial_window(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83489;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83490;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83490;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_send_active_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83492;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_send_active_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83492;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_send_delayed_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83493;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_send_delayed_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83493;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_send_fin(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_send_fin(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_send_loss_probe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83495;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_send_loss_probe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83495;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_send_probe0(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83496;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_send_probe0(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83496;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_send_syn_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83498;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_send_syn_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83498;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_send_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83499;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_send_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83499;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_send_window_probe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83500;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_send_window_probe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83500;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_skb_can_collapse(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83502;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_skb_can_collapse(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83502;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_skb_collapse_tstamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83504;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_skb_collapse_tstamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83504;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_syn_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83505;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_syn_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83505;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_synack_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83507;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_synack_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83507;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sync_mss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83509;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sync_mss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83509;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_trim_head(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83514;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_trim_head(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83514;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_tso_segs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83516;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_tso_segs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83516;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_tsq_handler(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83517;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_tsq_handler(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83517;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_tsq_write(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83518;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_tsq_write(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83518;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_update_skb_after_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83520;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_update_skb_after_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83520;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_ao_calc_key_rsk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83523;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_ao_calc_key_rsk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83523;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_ao_calc_key_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83524;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_ao_calc_key_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83524;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_ao_hash_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83525;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_ao_hash_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83525;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_ao_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83526;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_ao_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83526;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_ao_lookup_rsk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83527;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_ao_lookup_rsk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83527;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_ao_synack_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83528;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_ao_synack_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83528;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v4_parse_ao(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83529;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v4_parse_ao(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83529;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_wfree(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83530;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_wfree(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83530;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_write_wakeup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83532;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_write_wakeup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83532;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_write_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83534;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_write_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83534;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_xmit_probe_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_xmit_probe_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_xmit_retransmit_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83537;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_xmit_retransmit_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83537;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tls_client_hello_psk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83643;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tls_client_hello_psk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83643;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tls_server_hello_psk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83652;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tls_server_hello_psk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83652;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tpacket_destruct_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83794;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tpacket_destruct_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83794;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tpacket_fill_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83796;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tpacket_fill_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=83796;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_kfree_skb_hit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84030;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_kfree_skb_hit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84030;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_try_fill_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84300;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_try_fill_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84300;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp4_csum_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp4_csum_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp4_hwcsum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84496;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp4_hwcsum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84496;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp4_lib_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84498;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp4_lib_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84498;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp4_lib_lookup2(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84500;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp4_lib_lookup2(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84500;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp4_lib_lookup_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84502;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp4_lib_lookup_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84502;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp4_proc_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84504;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp4_proc_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84504;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp4_proc_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84506;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp4_proc_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84506;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp6_csum_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84508;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp6_csum_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84508;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp6_set_csum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84510;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp6_set_csum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84510;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_abort(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84511;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_abort(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84511;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_cmsg_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84513;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_cmsg_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84513;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_destroy_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84514;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_destroy_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84514;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_destruct_common(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84515;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_destruct_common(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84515;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_destruct_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84516;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_destruct_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84516;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_disconnect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84517;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_disconnect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84517;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_ehashfn(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84519;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_ehashfn(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84519;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84522;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84522;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_flush_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84524;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_flush_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84524;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_get_next(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84527;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_get_next(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84527;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84528;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84528;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_init_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84530;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_init_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84530;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84531;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84531;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_lib_get_port(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84533;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_lib_get_port(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84533;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_lib_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84534;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_lib_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84534;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_lib_lport_inuse(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_lib_lport_inuse(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_lib_lport_inuse2(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84538;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_lib_lport_inuse2(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84538;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_lib_rehash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84540;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_lib_rehash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84540;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_lib_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84542;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_lib_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84542;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_lib_unhash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84543;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_lib_unhash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84543;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_pernet_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84544;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_pernet_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84544;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_pernet_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84545;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_pernet_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84545;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_pre_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84547;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_pre_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84547;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_push_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84548;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_push_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84548;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_queue_rcv_one_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84549;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_queue_rcv_one_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84549;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_queue_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84550;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_queue_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84550;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84551;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84551;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_read_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84552;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_read_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84552;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84553;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84553;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_rmem_release(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84555;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_rmem_release(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84555;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_send_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84557;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_send_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84557;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_set_csum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84563;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_set_csum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84563;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84564;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84564;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_sk_rx_dst_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84566;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_sk_rx_dst_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84566;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_skb_destructor(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84567;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_skb_destructor(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84567;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_splice_eof(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84568;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_splice_eof(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84568;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_unicast_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84572;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_unicast_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84572;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_v4_early_demux(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84573;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_v4_early_demux(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84573;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_v4_get_port(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84575;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_v4_get_port(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84575;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_v4_rehash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84576;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_v4_rehash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84576;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udplite6_proc_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84578;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udplite6_proc_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84578;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udplite6_proc_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84580;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udplite6_proc_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84580;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udplite_getfrag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84581;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udplite_getfrag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84581;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udplitev6_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84582;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udplitev6_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84582;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udplitev6_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84585;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udplitev6_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84585;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udplitev6_sk_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84586;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udplitev6_sk_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=84586;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_virtnet_send_command(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=85400;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_virtnet_send_command(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=85400;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_virtnet_send_rx_ctrl_coal_vq_cmd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=85402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_virtnet_send_rx_ctrl_coal_vq_cmd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=85402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_virtnet_send_rx_notf_coal_cmds(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=85404;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_virtnet_send_rx_notf_coal_cmds(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=85404;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_virtnet_send_tx_ctrl_coal_vq_cmd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=85405;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_virtnet_send_tx_ctrl_coal_vq_cmd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=85405;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_virtnet_xdp_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=85438;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_virtnet_xdp_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=85438;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_wireless_send_event(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=85844;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_wireless_send_event(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=85844;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xmit_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86398;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xmit_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86398;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_bind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_bind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_build_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86404;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_build_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86404;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_build_skb_zerocopy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86405;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_build_skb_zerocopy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86405;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_clear_pool_at_qid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86407;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_clear_pool_at_qid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86407;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_clear_rx_need_wakeup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86408;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_clear_rx_need_wakeup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86408;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_clear_tx_need_wakeup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86409;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_clear_tx_need_wakeup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86409;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_consume_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86410;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_consume_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86410;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86413;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86413;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86414;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86414;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_destruct_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86415;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_destruct_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86415;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_generic_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86416;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_generic_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86416;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_get_pool_from_qid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86418;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_get_pool_from_qid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86418;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86419;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86419;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_mmap(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86421;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_mmap(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86421;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86422;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86422;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_net_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86423;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_net_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86423;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_poll(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86426;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_poll(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86426;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_rcv_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86427;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_rcv_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86427;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86428;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86428;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_reg_pool_at_qid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86430;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_reg_pool_at_qid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86430;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_release(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86431;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_release(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86431;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86432;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86432;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_set_rx_need_wakeup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86433;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_set_rx_need_wakeup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86433;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_set_tx_need_wakeup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86434;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_set_tx_need_wakeup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86434;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86435;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86435;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_tx_completed(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86437;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_tx_completed(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86437;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_tx_peek_desc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86439;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_tx_peek_desc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86439;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_tx_peek_release_desc_batch(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86441;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_tx_peek_release_desc_batch(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86441;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_tx_release(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86442;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_tx_release(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86442;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_uses_need_wakeup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86443;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_uses_need_wakeup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=86443;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_____ip_mc_inc_group(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=93167;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_____ip_mc_inc_group(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=93167;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___genl_sk_priv_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=93979;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___genl_sk_priv_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=93979;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___icmp_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94146;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___icmp_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94146;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip6addrlbl_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94198;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip6addrlbl_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94198;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip_mc_dec_group(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94200;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip_mc_dec_group(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94200;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip_mc_inc_group(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94201;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip_mc_inc_group(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94201;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip_mc_join_group(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94203;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip_mc_join_group(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94203;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___iptunnel_pull_header(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94205;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___iptunnel_pull_header(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94205;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ipv6_addr_label(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94207;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ipv6_addr_label(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94207;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_pm_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94315;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_pm_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94315;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_subflow_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94317;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_subflow_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94317;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_subflow_fully_established(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_subflow_fully_established(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_sync_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94319;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_sync_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94319;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___neigh_event_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94330;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___neigh_event_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94330;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_consume_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94511;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_consume_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94511;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_inet_sk_error_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94550;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_inet_sk_error_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94550;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_inet_sock_set_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94551;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_inet_sock_set_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94551;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_kfree_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94555;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_kfree_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94555;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_neigh_event_send_dead(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94581;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_neigh_event_send_dead(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94581;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_neigh_event_send_done(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94582;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_neigh_event_send_done(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94582;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_net_dev_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94587;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_net_dev_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94587;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_net_dev_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94588;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_net_dev_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94588;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_net_dev_xmit_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94589;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_net_dev_xmit_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94589;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_netif_receive_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94590;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_netif_receive_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94590;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_netif_receive_skb_entry(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94591;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_netif_receive_skb_entry(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94591;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_netif_receive_skb_list_entry(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94593;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_netif_receive_skb_list_entry(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94593;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_sk_data_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94623;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_sk_data_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94623;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_skb_copy_datagram_iovec(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94624;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_skb_copy_datagram_iovec(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94624;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_sock_exceed_buf_limit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94625;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_sock_exceed_buf_limit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94625;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_sock_rcvqueue_full(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94626;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_sock_rcvqueue_full(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94626;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_sock_recv_length(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94627;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_sock_recv_length(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94627;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_sock_send_length(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94628;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_sock_send_length(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94628;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_tcp_bad_csum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94637;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_tcp_bad_csum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94637;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_tcp_cong_state_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94638;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_tcp_cong_state_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94638;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_tcp_destroy_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94639;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_tcp_destroy_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94639;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_tcp_probe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94640;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_tcp_probe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94640;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_tcp_rcv_space_adjust(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94641;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_tcp_rcv_space_adjust(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94641;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_tcp_receive_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94642;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_tcp_receive_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94642;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_tcp_retransmit_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94643;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_tcp_retransmit_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94643;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_tcp_retransmit_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94644;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_tcp_retransmit_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94644;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_tcp_send_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94645;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_tcp_send_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94645;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_tls_alert_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94646;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_tls_alert_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94646;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_tls_alert_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94647;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_tls_alert_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94647;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_udp_fail_queue_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94649;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_udp_fail_queue_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94649;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_xdp_devmap_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94660;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_xdp_devmap_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94660;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sk_attach_prog(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sk_attach_prog(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sk_queue_drop_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94829;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sk_queue_drop_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94829;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_flow_dissect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94835;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_flow_dissect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94835;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_flow_dissect_arp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94837;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_flow_dissect_arp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94837;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_flow_dissect_gre(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_flow_dissect_gre(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_flow_get_ports(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94841;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_flow_get_ports(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94841;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_free_datagram_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94842;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_free_datagram_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94842;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_get_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94843;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_get_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94843;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_get_hash_symmetric(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94844;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_get_hash_symmetric(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94844;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_get_poff(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94846;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_get_poff(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94846;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_recv_datagram(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94848;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_recv_datagram(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94848;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_try_recv_datagram(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94850;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_try_recv_datagram(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94850;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_try_recv_from_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94851;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_try_recv_from_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94851;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_wait_for_more_packets(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94853;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_wait_for_more_packets(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94853;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___tcp_ack_snd_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94931;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___tcp_ack_snd_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94931;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___tcp_ecn_check_ce(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94932;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___tcp_ecn_check_ce(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94932;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___tcp_get_metrics(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94934;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___tcp_get_metrics(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=94934;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_consume_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95042;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_consume_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95042;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_inet_sk_error_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95101;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_inet_sk_error_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95101;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_inet_sock_set_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95103;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_inet_sock_set_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95103;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_kfree_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95111;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_kfree_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95111;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_neigh_event_send_dead(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95154;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_neigh_event_send_dead(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95154;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_neigh_event_send_done(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95155;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_neigh_event_send_done(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95155;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_net_dev_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95163;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_net_dev_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95163;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_net_dev_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95165;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_net_dev_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95165;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_net_dev_xmit_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95167;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_net_dev_xmit_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95167;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_netif_receive_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95168;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_netif_receive_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95168;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_netif_receive_skb_entry(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95169;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_netif_receive_skb_entry(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95169;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_netif_receive_skb_list_entry(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95171;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_netif_receive_skb_list_entry(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95171;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_sk_data_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95219;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_sk_data_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95219;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_skb_copy_datagram_iovec(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95221;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_skb_copy_datagram_iovec(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95221;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_sock_exceed_buf_limit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95223;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_sock_exceed_buf_limit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95223;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_sock_rcvqueue_full(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95225;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_sock_rcvqueue_full(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95225;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_sock_recv_length(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95227;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_sock_recv_length(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95227;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_sock_send_length(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95228;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_sock_send_length(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95228;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_tcp_bad_csum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95242;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_tcp_bad_csum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95242;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_tcp_cong_state_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95244;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_tcp_cong_state_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95244;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_tcp_destroy_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95246;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_tcp_destroy_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95246;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_tcp_probe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95247;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_tcp_probe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95247;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_tcp_rcv_space_adjust(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95248;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_tcp_rcv_space_adjust(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95248;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_tcp_receive_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95249;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_tcp_receive_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95249;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_tcp_retransmit_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95251;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_tcp_retransmit_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95251;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_tcp_retransmit_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95253;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_tcp_retransmit_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95253;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_tcp_send_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95254;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_tcp_send_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95254;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_tls_alert_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95256;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_tls_alert_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95256;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_tls_alert_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95257;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_tls_alert_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95257;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_udp_fail_queue_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95261;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_udp_fail_queue_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95261;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_xdp_devmap_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95280;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_xdp_devmap_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95280;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_aa_label_sk_perm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95694;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_aa_label_sk_perm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95694;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_aa_sk_perm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95718;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_aa_sk_perm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95718;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_aa_sock_file_perm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95719;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_aa_sock_file_perm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=95719;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_calipso_opt_update(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98495;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_calipso_opt_update(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98495;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_calipso_req_delattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98498;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_calipso_req_delattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98498;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_calipso_req_setattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98500;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_calipso_req_setattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98500;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_calipso_skbuff_delattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98501;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_calipso_skbuff_delattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98501;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_calipso_skbuff_optptr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98502;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_calipso_skbuff_optptr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98502;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_calipso_skbuff_setattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98504;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_calipso_skbuff_setattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98504;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_calipso_sock_delattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98505;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_calipso_sock_delattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98505;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_calipso_sock_getattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98506;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_calipso_sock_getattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98506;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_calipso_sock_setattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98508;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_calipso_sock_setattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98508;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_calipso_validate(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98512;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_calipso_validate(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98512;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_compat_ipv6_get_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98993;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_compat_ipv6_get_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98993;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_compat_ipv6_mcast_join_leave(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98994;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_compat_ipv6_mcast_join_leave(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98994;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_compat_ipv6_set_mcast_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98995;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_compat_ipv6_set_mcast_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=98995;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_cubictcp_acked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99530;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_cubictcp_acked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99530;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_cubictcp_cong_avoid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99532;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_cubictcp_cong_avoid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99532;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_cubictcp_cwnd_event(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99534;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_cubictcp_cwnd_event(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99534;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_cubictcp_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99535;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_cubictcp_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99535;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_cubictcp_recalc_ssthresh(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_cubictcp_recalc_ssthresh(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_cubictcp_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99539;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_cubictcp_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99539;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_devlink_dpipe_action_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99987;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_devlink_dpipe_action_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99987;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_devlink_dpipe_entry_ctx_append(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99991;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_devlink_dpipe_entry_ctx_append(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99991;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_devlink_dpipe_entry_ctx_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99993;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_devlink_dpipe_entry_ctx_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99993;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_devlink_dpipe_entry_ctx_prepare(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99994;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_devlink_dpipe_entry_ctx_prepare(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99994;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_devlink_dpipe_entry_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99996;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_devlink_dpipe_entry_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=99996;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_devlink_dpipe_match_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100000;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_devlink_dpipe_match_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100000;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_devlink_dpipe_send_and_alloc_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100002;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_devlink_dpipe_send_and_alloc_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100002;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_devlink_dpipe_table_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100006;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_devlink_dpipe_table_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100006;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_devlink_dpipe_value_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100010;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_devlink_dpipe_value_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100010;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_devlink_fmsg_prepare_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100030;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_devlink_fmsg_prepare_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100030;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_devlink_nl_dpipe_entries_get_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100062;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_devlink_nl_dpipe_entries_get_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100062;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_devlink_nl_dpipe_headers_get_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100063;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_devlink_nl_dpipe_headers_get_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100063;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_devlink_nl_dpipe_table_counters_set_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100064;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_devlink_nl_dpipe_table_counters_set_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100064;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_devlink_nl_dpipe_table_get_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100065;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_devlink_nl_dpipe_table_get_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100065;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_do_ipv6_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100645;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_do_ipv6_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100645;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_do_ipv6_mcast_group_source(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100646;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_do_ipv6_mcast_group_source(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100646;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_do_ipv6_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100647;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_do_ipv6_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=100647;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_eafnosupport_ip6_del_rt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101381;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_eafnosupport_ip6_del_rt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101381;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_eafnosupport_ip6_mtu_from_fib6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101382;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_eafnosupport_ip6_mtu_from_fib6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101382;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_eafnosupport_ipv6_dev_find(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101383;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_eafnosupport_ipv6_dev_find(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101383;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_eafnosupport_ipv6_dst_lookup_flow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101385;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_eafnosupport_ipv6_dst_lookup_flow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101385;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_eafnosupport_ipv6_fragment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101386;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_eafnosupport_ipv6_fragment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101386;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_eafnosupport_ipv6_route_input(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_eafnosupport_ipv6_route_input(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_espintcp_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101786;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_espintcp_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101786;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_espintcp_data_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101787;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_espintcp_data_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101787;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_espintcp_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101788;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_espintcp_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101788;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_espintcp_init_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101790;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_espintcp_init_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101790;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_espintcp_parse(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101792;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_espintcp_parse(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101792;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_espintcp_poll(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101793;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_espintcp_poll(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101793;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_espintcp_push_msgs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101794;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_espintcp_push_msgs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101794;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_espintcp_push_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101795;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_espintcp_push_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101795;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_espintcp_queue_out(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101796;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_espintcp_queue_out(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101796;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_espintcp_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101798;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_espintcp_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101798;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_espintcp_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101799;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_espintcp_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101799;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_espintcp_release(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_espintcp_release(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_espintcp_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101801;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_espintcp_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101801;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_espintcp_sendskmsg_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101803;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_espintcp_sendskmsg_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101803;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_espintcp_write_space(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101805;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_espintcp_write_space(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=101805;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_genl_sk_priv_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=103244;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_genl_sk_priv_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=103244;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmp_build_probe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104047;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmp_build_probe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104047;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmp_discard(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104048;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmp_discard(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104048;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmp_echo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104049;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmp_echo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104049;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmp_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104050;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmp_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104050;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmp_glue_bits(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104052;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmp_glue_bits(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104052;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmp_ndo_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104055;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmp_ndo_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104055;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmp_out_count(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104057;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmp_out_count(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104057;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmp_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104060;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmp_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104060;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmp_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104061;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmp_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104061;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmp_reply(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104063;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmp_reply(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104063;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmp_route_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104065;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmp_route_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104065;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmp_sk_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104066;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmp_sk_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104066;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmp_socket_deliver(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104068;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmp_socket_deliver(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104068;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmp_timestamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104069;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmp_timestamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104069;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmp_unreach(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104070;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmp_unreach(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104070;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmpv4_xrlim_allow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104072;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmpv4_xrlim_allow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104072;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_igmp_send_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104127;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_igmp_send_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104127;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_igmpv3_send_cr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104136;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_igmpv3_send_cr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104136;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_igmpv3_send_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104138;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_igmpv3_send_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104138;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_igmpv3_sendpack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104139;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_igmpv3_sendpack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104139;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet6_bind_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104240;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet6_bind_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104240;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet6_cleanup_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104241;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet6_cleanup_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104241;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet6_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104251;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet6_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104251;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet6_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104255;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet6_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104255;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet6_sk_rebuild_header(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104256;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet6_sk_rebuild_header(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104256;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet6_sock_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104257;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet6_sock_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104257;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_lhash2_bucket_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_lhash2_bucket_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_lookup_run_sk_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104311;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_lookup_run_sk_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104311;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_reqsk_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104331;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_reqsk_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104331;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_parse_tlv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_parse_tlv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_ra_control(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104802;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_ra_control(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104802;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_tlvopt_unknown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104804;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_tlvopt_unknown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104804;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_tun_build_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104806;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_tun_build_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104806;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_tun_fill_encap_info(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104809;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_tun_fill_encap_info(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104809;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6addrlbl_dump(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104812;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6addrlbl_dump(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104812;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6addrlbl_fill(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104814;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6addrlbl_fill(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104814;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6addrlbl_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104815;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6addrlbl_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104815;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6addrlbl_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104816;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6addrlbl_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104816;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6addrlbl_net_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104817;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6addrlbl_net_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104817;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6addrlbl_newdel(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104818;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6addrlbl_newdel(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104818;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_check_mc_rcu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104820;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_check_mc_rcu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104820;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_forward(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104821;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_forward(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104821;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_forward_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104822;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_forward_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104822;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_icmp_error_rfc4884(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104824;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_icmp_error_rfc4884(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104824;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_ma_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104825;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_ma_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104825;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_add_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_add_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_check_igmp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104828;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_check_igmp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104828;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_clear_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104830;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_clear_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104830;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_del1_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104832;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_del1_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104832;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_del_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104833;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_del_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104833;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_destroy_dev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104834;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_destroy_dev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104834;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_down(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104835;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_down(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104835;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_drop_socket(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104836;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_drop_socket(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104836;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_find_dev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104838;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_find_dev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104838;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_gsfget(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_gsfget(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_inc_group(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104841;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_inc_group(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104841;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_init_dev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104842;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_init_dev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104842;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_join_group(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104844;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_join_group(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104844;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_join_group_ssm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104845;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_join_group_ssm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104845;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_leave_group(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104846;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_leave_group(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104846;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_leave_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104848;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_leave_src(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104848;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_msfget(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104850;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_msfget(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104850;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104852;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104852;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_remap(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104853;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_remap(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104853;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_sf_allow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104855;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_sf_allow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104855;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_source(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104857;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_source(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104857;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_unmap(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104858;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_unmap(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104858;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_up(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104859;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_up(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104859;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mc_validate_checksum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104860;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mc_validate_checksum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104860;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_tun_build_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104861;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_tun_build_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104861;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_tun_fill_encap_info(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104866;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_tun_fill_encap_info(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104866;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_tun_fill_encap_opts(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104868;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_tun_fill_encap_opts(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104868;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_tunnel_parse_protocol(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104883;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_tunnel_parse_protocol(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104883;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_iptunnel_handle_offloads(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104890;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_iptunnel_handle_offloads(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104890;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_iptunnel_metadata_reply(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104892;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_iptunnel_metadata_reply(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104892;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_iptunnel_pmtud_build_icmp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104894;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_iptunnel_pmtud_build_icmp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104894;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_iptunnel_pmtud_build_icmpv6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104895;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_iptunnel_pmtud_build_icmpv6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104895;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_iptunnel_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104897;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_iptunnel_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104897;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_skb_to_auditdata(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104903;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_skb_to_auditdata(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104903;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_sysctl_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_sysctl_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104904;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_sysctl_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104905;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_sysctl_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104905;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_addr_label(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104907;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_addr_label(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104907;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_dest_hao(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104912;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_dest_hao(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104912;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_destopt_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104913;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_destopt_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104913;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_dup_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104915;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_dup_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104915;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_get_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104918;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_get_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104918;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104919;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104919;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_hop_ra(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104920;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_hop_ra(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104920;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_init_mibs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104921;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_init_mibs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104921;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_mcast_join_leave(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104922;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_mcast_join_leave(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104922;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_opt_accepted(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104925;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_opt_accepted(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104925;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_parse_hopopts(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104926;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_parse_hopopts(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104926;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_push_exthdr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104928;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_push_exthdr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104928;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_push_frag_opts(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104930;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_push_frag_opts(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104930;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_push_nfrag_opts(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104932;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_push_nfrag_opts(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104932;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_push_rthdr0(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104934;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_push_rthdr0(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104934;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_push_rthdr4(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104935;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_push_rthdr4(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104935;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_renew_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104939;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_renew_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104939;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_route_input(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104940;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_route_input(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104940;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_rpl_srh_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104941;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_rpl_srh_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104941;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_rthdr_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104942;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_rthdr_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104942;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_set_mcast_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104943;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_set_mcast_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104943;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_set_opt_hdr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104944;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_set_opt_hdr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104944;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104945;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104945;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_skb_to_auditdata(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104946;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_skb_to_auditdata(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104946;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_srh_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104947;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_srh_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104947;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_update_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104948;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_update_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=104948;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_lwtunnel_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=105774;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_lwtunnel_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=105774;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_can_accept_new_subflow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106423;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_can_accept_new_subflow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106423;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_event(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106425;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_event(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106425;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_event_add_subflow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106427;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_event_add_subflow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106427;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_event_addr_announced(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106429;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_event_addr_announced(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106429;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_event_addr_removed(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106431;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_event_addr_removed(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106431;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_event_pm_listener(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106433;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_event_pm_listener(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106433;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_event_put_token_and_ssk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106435;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_event_put_token_and_ssk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106435;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_init_sched(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106439;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_init_sched(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106439;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_lookup_anno_list_by_saddr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106443;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_lookup_anno_list_by_saddr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106443;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_nl_fill_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106445;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_nl_fill_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106445;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_nl_mcast_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106447;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_nl_mcast_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106447;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_nl_remove_id_zero_address(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106449;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_nl_remove_id_zero_address(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106449;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_nl_remove_subflow_and_signal_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106451;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_nl_remove_subflow_and_signal_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106451;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_add_addr_echoed(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106453;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_add_addr_echoed(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106453;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_add_addr_received(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106455;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_add_addr_received(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106455;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_add_addr_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106456;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_add_addr_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106456;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_add_addr_signal(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106458;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_add_addr_signal(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106458;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_addr_families_match(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106461;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_addr_families_match(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106461;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_alloc_anno_list(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106463;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_alloc_anno_list(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106463;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_allow_new_subflow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106465;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_allow_new_subflow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106465;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_announce_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106467;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_announce_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106467;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_connection_closed(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106468;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_connection_closed(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106468;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_create_subflow_or_signal_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106469;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_create_subflow_or_signal_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106469;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_data_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106470;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_data_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106470;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_data_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106471;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_data_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106471;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_del_add_timer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106473;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_del_add_timer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106473;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_free_anno_list(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106474;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_free_anno_list(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106474;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_fully_established(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106476;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_fully_established(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106476;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_get_add_addr_accept_max(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106478;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_get_add_addr_accept_max(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106478;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_get_add_addr_signal_max(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106479;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_get_add_addr_signal_max(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106479;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_get_flags_and_ifindex_by_id(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106480;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_get_flags_and_ifindex_by_id(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106480;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_get_local_addr_max(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106481;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_get_local_addr_max(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106481;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_get_local_id(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106483;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_get_local_id(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106483;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_get_subflows_max(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106484;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_get_subflows_max(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106484;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_is_backup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106489;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_is_backup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106489;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_mp_fail_received(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106491;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_mp_fail_received(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106491;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_mp_prio_received(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106493;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_mp_prio_received(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106493;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_new_connection(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106495;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_new_connection(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106495;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_add_addr_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106496;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_add_addr_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106496;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_add_addr_received(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106497;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_add_addr_received(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106497;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_addr_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106498;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_addr_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106498;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_append_new_local_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106500;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_append_new_local_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106500;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_check_work_pending(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106501;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_check_work_pending(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106501;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_create_listen_socket(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106503;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_create_listen_socket(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106503;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_del_addr_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106504;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_del_addr_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106504;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_flush_addrs_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106505;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_flush_addrs_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106505;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_get_addr_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106506;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_get_addr_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106506;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_get_addr_dumpit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106507;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_get_addr_dumpit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106507;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_get_flags_and_ifindex_by_id(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106508;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_get_flags_and_ifindex_by_id(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106508;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_get_limits_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106509;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_get_limits_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106509;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_get_local_id(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106510;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_get_local_id(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106510;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_is_backup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106512;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_is_backup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106512;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_is_init_remote_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106514;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_is_init_remote_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106514;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_mp_prio_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106516;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_mp_prio_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106516;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_rm_addr_or_subflow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106518;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_rm_addr_or_subflow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106518;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_set_flags(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106520;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_set_flags(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106520;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_set_flags_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106521;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_set_flags_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106521;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_set_limits_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106522;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_set_limits_doit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106522;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_subflow_chk_stale(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106524;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_subflow_chk_stale(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106524;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_nl_work(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106525;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_nl_work(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106525;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_parse_entry(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106529;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_parse_entry(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106529;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_remove_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106533;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_remove_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106533;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_remove_addrs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106535;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_remove_addrs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106535;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_remove_addrs_and_subflows(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_remove_addrs_and_subflows(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_remove_anno_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106538;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_remove_anno_addr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106538;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_rm_addr_received(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106540;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_rm_addr_received(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106540;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_rm_addr_signal(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106542;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_rm_addr_signal(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106542;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_schedule_work(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106544;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_schedule_work(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106544;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_set_flags(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106545;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_set_flags(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106545;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_sport_in_anno_list(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106547;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_sport_in_anno_list(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106547;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_subflow_check_next(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106549;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_subflow_check_next(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106549;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_subflow_chk_stale(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106550;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_subflow_chk_stale(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106550;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pm_subflow_established(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106551;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pm_subflow_established(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106551;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_propagate_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106553;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_propagate_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106553;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_release_sched(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106556;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_release_sched(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106556;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_sched_default_get_subflow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106558;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_sched_default_get_subflow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106558;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_sched_get_retrans(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106562;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_sched_get_retrans(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106562;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_sched_get_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106563;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_sched_get_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106563;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_space(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106566;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_space(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106566;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_stream_memory_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106567;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_stream_memory_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106567;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_subflow_create_socket(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106569;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_subflow_create_socket(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106569;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_subflow_data_available(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106570;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_subflow_data_available(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106570;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_subflow_drop_ctx(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106572;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_subflow_drop_ctx(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106572;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_subflow_init_cookie_req(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106575;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_subflow_init_cookie_req(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106575;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_subflow_queue_clean(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106577;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_subflow_queue_clean(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106577;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_subflow_reqsk_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106578;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_subflow_reqsk_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106578;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_subflow_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106579;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_subflow_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106579;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_subflow_set_scheduled(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106581;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_subflow_set_scheduled(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106581;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_userspace_pm_active(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106584;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_userspace_pm_active(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106584;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcpv6_handle_mapped(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106586;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcpv6_handle_mapped(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106586;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_neigh_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106931;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_neigh_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106931;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_net_failover_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106976;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_net_failover_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106976;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_net_failover_xmit_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106979;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_net_failover_xmit_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=106979;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_nf_ip6_check_hbh_len(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=107051;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_nf_ip6_check_hbh_len(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=107051;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_nf_ip6_checksum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=107053;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_nf_ip6_checksum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=107053;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_nf_ip_checksum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=107054;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_nf_ip_checksum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=107054;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_consume_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108138;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_consume_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108138;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_inet_sk_error_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108161;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_inet_sk_error_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108161;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_inet_sock_set_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108162;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_inet_sock_set_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108162;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_kfree_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108168;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_kfree_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108168;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_net_dev_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108190;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_net_dev_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108190;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_net_dev_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108192;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_net_dev_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108192;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_net_dev_xmit_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108193;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_net_dev_xmit_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108193;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_sk_data_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108216;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_sk_data_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108216;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_skb_copy_datagram_iovec(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108217;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_skb_copy_datagram_iovec(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108217;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_sock_exceed_buf_limit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108218;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_sock_exceed_buf_limit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108218;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_sock_msg_length(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108219;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_sock_msg_length(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108219;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_sock_rcvqueue_full(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108220;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_sock_rcvqueue_full(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108220;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_tcp_cong_state_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108226;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_tcp_cong_state_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108226;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_tcp_event_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108227;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_tcp_event_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108227;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_tcp_event_sk_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108228;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_tcp_event_sk_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108228;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_tcp_event_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108229;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_tcp_event_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108229;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_tcp_probe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108230;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_tcp_probe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108230;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_tcp_retransmit_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108231;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_tcp_retransmit_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108231;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_udp_fail_queue_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108233;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_udp_fail_queue_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108233;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_xdp_devmap_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108241;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_xdp_devmap_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=108241;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_raw_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109222;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_raw_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109222;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_raw_hash_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109223;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_raw_hash_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109223;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_raw_icmp_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109225;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_raw_icmp_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109225;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_raw_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109235;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_raw_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109235;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_raw_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109236;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_raw_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109236;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_raw_send_hdrinc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109238;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_raw_send_hdrinc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109238;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_raw_seticmpfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109244;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_raw_seticmpfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109244;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_raw_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109245;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_raw_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109245;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_raw_sk_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109246;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_raw_sk_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109246;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_raw_unhash_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109248;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_raw_unhash_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109248;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_reqsk_fastopen_remove(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109628;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_reqsk_fastopen_remove(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109628;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_reqsk_queue_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109630;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_reqsk_queue_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=109630;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sch_frag_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=110047;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sch_frag_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=110047;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sch_frag_xmit_hook(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=110048;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sch_frag_xmit_hook(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=110048;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sfp_unregister_socket(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=110760;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sfp_unregister_socket(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=110760;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_attach_filter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111062;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_attach_filter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111062;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_detach_filter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111063;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_detach_filter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111063;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_filter_charge(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111065;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_filter_charge(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111065;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_filter_trim_cap(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111070;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_filter_trim_cap(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111070;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_filter_uncharge(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111072;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_filter_uncharge(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111072;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_get_filter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111074;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_get_filter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111074;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111076;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111076;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_reuseport_attach_filter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111084;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_reuseport_attach_filter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111084;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_udp_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111103;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_udp_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111103;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_copy_and_csum_datagram_msg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111105;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_copy_and_csum_datagram_msg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111105;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_copy_and_hash_datagram_iter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111107;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_copy_and_hash_datagram_iter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111107;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_do_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111112;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_do_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111112;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_flow_dissect_ct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111114;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_flow_dissect_ct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111114;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_flow_dissect_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111116;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_flow_dissect_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111116;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_flow_dissect_meta(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111117;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_flow_dissect_meta(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111117;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_flow_dissect_tunnel_info(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111118;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_flow_dissect_tunnel_info(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111118;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_flow_get_icmp_tci(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111122;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_flow_get_icmp_tci(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111122;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_free_datagram(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111123;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_free_datagram(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111123;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_get_hash_perturb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111125;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_get_hash_perturb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111125;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_get_poff(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111126;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_get_poff(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111126;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_kill_datagram(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111128;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_kill_datagram(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111128;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_recv_datagram(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111130;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_recv_datagram(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111130;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_tunnel_check_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111132;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_tunnel_check_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111132;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_edemux(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111307;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_edemux(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111307;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_gen_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111310;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_gen_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111310;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sol_ip_sockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111321;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sol_ip_sockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111321;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sol_ipv6_sockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111322;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sol_ipv6_sockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111322;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sol_socket_sockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111323;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sol_socket_sockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111323;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sol_tcp_sockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111324;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sol_tcp_sockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111324;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_subflow_syn_recv_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111547;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_subflow_syn_recv_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111547;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_subflow_v4_send_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111555;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_subflow_v4_send_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111555;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_subflow_v6_send_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111560;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_subflow_v6_send_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=111560;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp6_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112018;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp6_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112018;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp6_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112019;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp6_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112019;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp6_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112020;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp6_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112020;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_abort_override(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112022;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_abort_override(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112022;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112024;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112024;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ack_tstamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112026;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ack_tstamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112026;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ack_update_rtt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112028;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ack_update_rtt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112028;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_add_reno_sack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112030;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_add_reno_sack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112030;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ca_openreq_child(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112032;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ca_openreq_child(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112032;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_check_dsack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112034;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_check_dsack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112034;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_check_req(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112036;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_check_req(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112036;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_check_sack_reordering(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112038;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_check_sack_reordering(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112038;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_check_space(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112039;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_check_space(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112039;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_child_process(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112041;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_child_process(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112041;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_clean_rtx_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112043;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_clean_rtx_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112043;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_clear_retrans(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112044;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_clear_retrans(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112044;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_collapse(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112046;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_collapse(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112046;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_collapse_one(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112048;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_collapse_one(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112048;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_conn_request(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112050;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_conn_request(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112050;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_create_openreq_child(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112052;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_create_openreq_child(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112052;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_cwnd_reduction(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112054;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_cwnd_reduction(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112054;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_data_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112055;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_data_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112055;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_data_queue_ofo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112056;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_data_queue_ofo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112056;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_data_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112057;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_data_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112057;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_done_with_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112061;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_done_with_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112061;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_dsack_extend(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112063;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_dsack_extend(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112063;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_enter_cwr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112064;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_enter_cwr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112064;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_enter_loss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112065;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_enter_loss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112065;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_enter_recovery(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112067;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_enter_recovery(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112067;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_event_data_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112068;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_event_data_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112068;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_fastopen_cache_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112070;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_fastopen_cache_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112070;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_fastopen_cache_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112072;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_fastopen_cache_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112072;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_fastretrans_alert(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112074;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_fastretrans_alert(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112074;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_fin(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112075;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_fin(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112075;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_finish_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112076;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_finish_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112076;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_get_metrics(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112078;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_get_metrics(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112078;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_get_syncookie_mss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112080;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_get_syncookie_mss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112080;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_grow_window(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112082;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_grow_window(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112082;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_identify_packet_loss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112084;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_identify_packet_loss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112084;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_init_cwnd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112086;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_init_cwnd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112086;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_init_metrics(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112087;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_init_metrics(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112087;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_init_transfer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112089;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_init_transfer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112089;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_initialize_rcv_mss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112090;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_initialize_rcv_mss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112090;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_is_ulp_esp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112091;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_is_ulp_esp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112091;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_mark_head_lost(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112093;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_mark_head_lost(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112093;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_mark_skb_lost(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112094;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_mark_skb_lost(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112094;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_match_skb_to_sack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112096;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_match_skb_to_sack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112096;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_metrics_fill_info(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112099;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_metrics_fill_info(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112099;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_metrics_flush_all(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112100;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_metrics_flush_all(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112100;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_metrics_nl_cmd_del(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112102;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_metrics_nl_cmd_del(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112102;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_metrics_nl_cmd_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112103;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_metrics_nl_cmd_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112103;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_metrics_nl_dump(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112104;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_metrics_nl_dump(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112104;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_newly_delivered(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112107;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_newly_delivered(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112107;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_newreno_mark_lost(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112109;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_newreno_mark_lost(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112109;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_non_congestion_loss_retransmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112110;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_non_congestion_loss_retransmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112110;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_ofo_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112111;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_ofo_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112111;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_oow_rate_limited(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112113;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_oow_rate_limited(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112113;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_openreq_init_rwin(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112115;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_openreq_init_rwin(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112115;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_parse_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112121;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_parse_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112121;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_peer_is_proven(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112123;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_peer_is_proven(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112123;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_process_tlp_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112125;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_process_tlp_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112125;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_prune_ofo_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112127;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_prune_ofo_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112127;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_queue_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112129;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_queue_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112129;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rack_advance(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112131;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rack_advance(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112131;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rack_detect_loss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112133;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rack_detect_loss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112133;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rack_mark_lost(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112134;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rack_mark_lost(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112134;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rack_reo_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112135;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rack_reo_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112135;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rack_skb_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112137;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rack_skb_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112137;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rack_update_reo_wnd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112139;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rack_update_reo_wnd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112139;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rbtree_insert(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112141;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rbtree_insert(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112141;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rcv_established(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112142;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rcv_established(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112142;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rcv_fastopen_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112144;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rcv_fastopen_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112144;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rcv_space_adjust(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112145;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rcv_space_adjust(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112145;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rcv_spurious_retrans(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112146;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rcv_spurious_retrans(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112146;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rcv_state_process(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112147;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rcv_state_process(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112147;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rcv_synrecv_state_fastopen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112148;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rcv_synrecv_state_fastopen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112148;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rcv_synsent_state_process(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112150;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rcv_synsent_state_process(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112150;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rearm_rto(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112151;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rearm_rto(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112151;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_release_cb_override(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112152;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_release_cb_override(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112152;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_reqsk_record_syn(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112154;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_reqsk_record_syn(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112154;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112155;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112155;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_rtt_estimator(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112157;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_rtt_estimator(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112157;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sack_compress_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112158;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sack_compress_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112158;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sack_new_ofo_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112159;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sack_new_ofo_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112159;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sacktag_one(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112161;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sacktag_one(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112161;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sacktag_walk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112163;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sacktag_walk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112163;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sacktag_write_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112165;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sacktag_write_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112165;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_send_challenge_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112166;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_send_challenge_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112166;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_send_dupack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112167;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_send_dupack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112167;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_send_rcvq(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112168;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_send_rcvq(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112168;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_shift_skb_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112170;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_shift_skb_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112170;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_shifted_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112172;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_shifted_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112172;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_simple_retransmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112173;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_simple_retransmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112173;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_skb_shift(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112175;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_skb_shift(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112175;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_sndbuf_expand(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112176;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_sndbuf_expand(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112176;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_syn_flood_action(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112178;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_syn_flood_action(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112178;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_synack_rtt_meas(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112180;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_synack_rtt_meas(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112180;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_time_wait(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112182;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_time_wait(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112182;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_timewait_state_process(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112184;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_timewait_state_process(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112184;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_try_coalesce(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112186;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_try_coalesce(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112186;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_try_keep_open(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112187;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_try_keep_open(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112187;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_try_rmem_schedule(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112189;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_try_rmem_schedule(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112189;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_try_to_open(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112191;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_try_to_open(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112191;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_try_undo_loss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112193;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_try_undo_loss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112193;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_try_undo_recovery(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112194;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_try_undo_recovery(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112194;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_twsk_destructor(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112195;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_twsk_destructor(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112195;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_undo_cwnd_reduction(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112198;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_undo_cwnd_reduction(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112198;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_update_metrics(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112199;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_update_metrics(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112199;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_update_pacing_rate(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112200;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_update_pacing_rate(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112200;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_urg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112202;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_urg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112202;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_validate_incoming(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112204;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_validate_incoming(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112204;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_xmit_recovery(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112206;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_xmit_recovery(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112206;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcpm_new(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112208;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcpm_new(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112208;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcpm_suck_dst(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112210;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcpm_suck_dst(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112210;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tls_alert_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112426;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tls_alert_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112426;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tls_alert_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112428;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tls_alert_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112428;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_get_offsets_net_dev_xmit_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112704;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_get_offsets_net_dev_xmit_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112704;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_consume_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112738;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_consume_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112738;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_inet_sk_error_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112761;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_inet_sk_error_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112761;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_inet_sock_set_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112762;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_inet_sock_set_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112762;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_kfree_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112768;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_kfree_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112768;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_net_dev_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112790;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_net_dev_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112790;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_net_dev_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112792;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_net_dev_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112792;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_net_dev_xmit_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112793;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_net_dev_xmit_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112793;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_sk_data_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112814;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_sk_data_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112814;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_skb_copy_datagram_iovec(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112815;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_skb_copy_datagram_iovec(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112815;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_sock_exceed_buf_limit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112816;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_sock_exceed_buf_limit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112816;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_sock_msg_length(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112817;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_sock_msg_length(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112817;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_sock_rcvqueue_full(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112818;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_sock_rcvqueue_full(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112818;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_tcp_cong_state_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112824;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_tcp_cong_state_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112824;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_tcp_event_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112825;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_tcp_event_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112825;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_tcp_event_sk_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112826;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_tcp_event_sk_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112826;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_tcp_event_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_tcp_event_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112827;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_tcp_probe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112828;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_tcp_probe(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112828;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_tcp_retransmit_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112829;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_tcp_retransmit_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112829;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_udp_fail_queue_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112831;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_udp_fail_queue_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112831;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_xdp_devmap_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_xdp_devmap_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=112839;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_msg_wait_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=113475;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_msg_wait_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=113475;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xfrm4_ipcomp_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=114605;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xfrm4_ipcomp_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=114605;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xfrm4_ipcomp_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=114606;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xfrm4_ipcomp_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=114606;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_map_sock_delete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=114876;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_map_sock_delete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=114876;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xsk_map_try_sock_delete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=114878;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xsk_map_try_sock_delete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=114878;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___dev_direct_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=115802;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___dev_direct_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=115802;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___dev_forward_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=115804;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___dev_forward_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=115804;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___dev_forward_skb2(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=115806;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___dev_forward_skb2(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=115806;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___dev_queue_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=115818;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___dev_queue_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=115818;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___dev_xmit_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=115829;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___dev_xmit_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=115829;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___inet6_lookup_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116204;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___inet6_lookup_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116204;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip4_datagram_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116280;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip4_datagram_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116280;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip6_datagram_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip6_datagram_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip6_dgram_sock_seq_show(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116283;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip6_dgram_sock_seq_show(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116283;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip6_flush_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116285;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip6_flush_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116285;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip6_make_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116287;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip6_make_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116287;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip_dev_find(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116289;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip_dev_find(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116289;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ip_sock_set_tos(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116290;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ip_sock_set_tos(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116290;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_check_push(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_check_push(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_clean_una(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116388;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_clean_una(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116388;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116390;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116390;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_close_ssk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116392;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_close_ssk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116392;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_data_acked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116393;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_data_acked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116393;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_destroy_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116394;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_destroy_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116394;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_error_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116395;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_error_report(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116395;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_finish_join(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116397;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_finish_join(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116397;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_init_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116398;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_init_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116398;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_move_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116400;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_move_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116400;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_move_skbs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116401;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_move_skbs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116401;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_move_skbs_from_subflow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116403;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_move_skbs_from_subflow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116403;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_nmpc_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116405;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_nmpc_sk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116405;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_ofo_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116406;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_ofo_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116406;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_push_pending(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116408;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_push_pending(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116408;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_retrans(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116409;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_retrans(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116409;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_retransmit_pending_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116410;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_retransmit_pending_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116410;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_setsockopt_set_val(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116412;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_setsockopt_set_val(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116412;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_subflow_push_pending(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116414;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_subflow_push_pending(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116414;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_subflow_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116415;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_subflow_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116415;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_unaccepted_force_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116416;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_unaccepted_force_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116416;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_update_rmem(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116417;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_update_rmem(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116417;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___mptcp_wr_shutdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116418;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___mptcp_wr_shutdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116418;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___netif_receive_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116477;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___netif_receive_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116477;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___netif_receive_skb_core(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116479;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___netif_receive_skb_core(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116479;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___netif_receive_skb_one_core(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116483;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___netif_receive_skb_one_core(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116483;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___netlink_sendskb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116505;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___netlink_sendskb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116505;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___netpoll_send_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116511;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___netpoll_send_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116511;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___nf_ip6_route(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116517;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___nf_ip6_route(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116517;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___ppp_xmit_process(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116601;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___ppp_xmit_process(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116601;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___probestub_mptcp_subflow_get_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116799;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___probestub_mptcp_subflow_get_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=116799;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sk_msg_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117057;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sk_msg_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117057;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sk_msg_free_partial(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117059;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sk_msg_free_partial(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117059;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___skb_udp_tunnel_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117061;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___skb_udp_tunnel_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117061;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sock_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117065;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sock_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117065;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sock_recv_cmsgs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117067;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sock_recv_cmsgs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117067;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sock_recv_timestamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117068;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sock_recv_timestamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117068;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sock_recv_wifi_status(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117069;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sock_recv_wifi_status(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117069;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sock_release(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117071;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sock_release(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117071;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sys_recvmsg_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117146;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sys_recvmsg_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117146;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sys_sendmsg_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117151;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sys_sendmsg_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117151;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___sys_shutdown_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117158;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___sys_shutdown_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117158;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___traceiter_mptcp_subflow_get_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117547;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___traceiter_mptcp_subflow_get_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117547;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___udp6_lib_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117691;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___udp6_lib_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117691;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___udp6_lib_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117693;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___udp6_lib_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117693;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___udp6_lib_mcast_deliver(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117695;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___udp6_lib_mcast_deliver(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117695;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___udp6_lib_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117696;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___udp6_lib_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117696;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___udp_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117698;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___udp_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117698;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___udpv4_gso_segment_csum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117700;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___udpv4_gso_segment_csum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117700;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___xdp_build_skb_from_frame(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117837;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___xdp_build_skb_from_frame(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117837;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe___xfrm6_udp_encap_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117850;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe___xfrm6_udp_encap_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117850;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_aa_audit_perm_mask(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117959;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_aa_audit_perm_mask(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=117959;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_aa_unix_label_sk_perm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=118041;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_aa_unix_label_sk_perm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=118041;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_aa_unix_sock_perm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=118047;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_aa_unix_sock_perm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=118047;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_audit_log_task(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=119885;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_audit_log_task(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=119885;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_br_ip6_fragment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=120946;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_br_ip6_fragment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=120946;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_calipso_optptr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121061;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_calipso_optptr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121061;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_call_fib6_multipath_entry_notifiers(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121070;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_call_fib6_multipath_entry_notifiers(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121070;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_call_trace_sock_recv_length(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121088;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_call_trace_sock_recv_length(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121088;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_call_trace_sock_send_length(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121089;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_call_trace_sock_send_length(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121089;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_cn_rx_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121436;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_cn_rx_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121436;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_compat_ip_get_mcast_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121502;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_compat_ip_get_mcast_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121502;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_compat_ip_mcast_join_leave(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121503;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_compat_ip_mcast_join_leave(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121503;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_compat_ip_set_mcast_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121504;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_compat_ip_set_mcast_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121504;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_compat_sock_ioctl_trans(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121515;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_compat_sock_ioctl_trans(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121515;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_cookie_tcp_reqsk_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121597;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_cookie_tcp_reqsk_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=121597;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dev_forward_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=122472;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dev_forward_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=122472;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dev_forward_skb_nomtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=122473;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dev_forward_skb_nomtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=122473;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dev_hard_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=122506;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dev_hard_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=122506;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dev_kfree_skb_any_reason(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=122518;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dev_kfree_skb_any_reason(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=122518;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dev_kfree_skb_irq_reason(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=122519;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dev_kfree_skb_irq_reason(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=122519;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dev_loopback_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=122522;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dev_loopback_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=122522;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_dev_queue_xmit_nit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=122550;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_dev_queue_xmit_nit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=122550;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_do_ip_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=123301;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_do_ip_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=123301;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_do_ip_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=123302;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_do_ip_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=123302;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_do_sock_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=123387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_do_sock_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=123387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_do_sock_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=123389;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_do_sock_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=123389;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_fib6_clean_all_skip_notify(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=125494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_fib6_clean_all_skip_notify(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=125494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_get_tcp6_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=126650;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_get_tcp6_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=126650;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmp6_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127126;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmp6_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127126;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmpv6_echo_reply(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127132;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmpv6_echo_reply(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127132;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmpv6_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127133;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmpv6_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127133;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmpv6_flow_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127136;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmpv6_flow_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127136;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmpv6_getfrag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127137;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmpv6_getfrag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127137;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmpv6_mask_allow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127140;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmpv6_mask_allow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127140;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmpv6_ndo_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127142;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmpv6_ndo_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127142;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmpv6_notify(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127144;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmpv6_notify(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127144;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmpv6_param_prob_reason(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127146;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmpv6_param_prob_reason(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127146;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmpv6_push_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127148;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmpv6_push_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127148;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmpv6_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127149;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmpv6_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127149;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmpv6_route_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127151;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmpv6_route_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127151;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_icmpv6_xrlim_allow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127153;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_icmpv6_xrlim_allow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127153;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet6_sk_rx_dst_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127266;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet6_sk_rx_dst_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127266;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet6_steal_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127268;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet6_steal_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127268;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_accept(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127288;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_accept(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127288;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_addr2sockaddr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127289;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_addr2sockaddr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127289;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_bind_conflict(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127291;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_bind_conflict(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127291;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_clear_xmit_timers(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127292;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_clear_xmit_timers(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127292;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_clear_xmit_timers_sync(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127293;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_clear_xmit_timers_sync(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127293;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_clone_lock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127295;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_clone_lock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127295;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_complete_hashdance(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127297;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_complete_hashdance(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127297;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_delete_keepalive_timer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127298;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_delete_keepalive_timer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127298;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_destroy_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127299;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_destroy_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127299;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_find_open_port(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127301;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_find_open_port(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127301;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_get_port(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127302;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_get_port(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127302;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_init_xmit_timers(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127304;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_init_xmit_timers(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127304;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_listen_start(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_listen_start(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_listen_stop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127306;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_listen_stop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127306;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_prepare_forced_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127307;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_prepare_forced_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127307;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_rebuild_route(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127309;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_rebuild_route(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127309;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_reqsk_queue_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127311;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_reqsk_queue_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127311;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_reqsk_queue_drop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127313;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_reqsk_queue_drop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127313;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_reqsk_queue_drop_and_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127314;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_reqsk_queue_drop_and_put(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127314;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_reqsk_queue_hash_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127316;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_reqsk_queue_hash_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127316;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_reset_keepalive_timer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_reset_keepalive_timer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_route_child_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127320;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_route_child_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127320;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_route_req(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127322;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_route_req(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127322;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_update_fastreuse(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127324;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_update_fastreuse(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127324;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_csk_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127325;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_csk_update_pmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127325;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_reqsk_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127374;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_reqsk_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127374;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_inet_sk_get_local_port_range(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127385;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_inet_sk_get_local_port_range(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=127385;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip4_datagram_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128295;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip4_datagram_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128295;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip4_datagram_release_cb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128296;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip4_datagram_release_cb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128296;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip4_frag_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128297;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip4_frag_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128297;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip4_frag_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128298;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip4_frag_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128298;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip4ip6_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128304;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip4ip6_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128304;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip4ip6_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip4ip6_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip4ip6_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128306;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip4ip6_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128306;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_append_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128311;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_append_data(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128311;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_autoflowlabel(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128313;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_autoflowlabel(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128313;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_copy_metadata(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128316;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_copy_metadata(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128316;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_cork_release(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_cork_release(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_datagram_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128319;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_datagram_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128319;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_datagram_connect_v6_only(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128320;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_datagram_connect_v6_only(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128320;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_datagram_dst_update(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128322;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_datagram_dst_update(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128322;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_datagram_recv_common_ctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128323;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_datagram_recv_common_ctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128323;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_datagram_recv_ctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128324;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_datagram_recv_ctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128324;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_datagram_recv_specific_ctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128325;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_datagram_recv_specific_ctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128325;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_datagram_release_cb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128326;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_datagram_release_cb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128326;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_datagram_send_ctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128328;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_datagram_send_ctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128328;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_dst_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128330;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_dst_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128330;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_dst_lookup_flow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128331;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_dst_lookup_flow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128331;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_dst_lookup_tail(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128333;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_dst_lookup_tail(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128333;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_err_gen_icmpv6_unreach(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128335;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_err_gen_icmpv6_unreach(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128335;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_finish_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128336;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_finish_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128336;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_finish_output2(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128337;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_finish_output2(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128337;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_flush_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_flush_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_forward(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128339;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_forward(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128339;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_forward_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128340;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_forward_finish(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128340;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_frag_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128342;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_frag_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128342;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_frag_next(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128344;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_frag_next(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128344;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_fraglist_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128346;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_fraglist_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128346;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_fraglist_prepare(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128348;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_fraglist_prepare(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128348;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_fragment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128349;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_fragment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128349;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_make_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128351;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_make_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128351;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128352;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128352;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_push_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128353;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_push_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128353;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_route_me_harder(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128355;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_route_me_harder(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128355;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_send_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128356;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_send_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128356;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_setup_cork(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128358;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_setup_cork(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128358;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_sk_dst_lookup_flow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128360;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_sk_dst_lookup_flow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128360;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128364;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128364;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6ip6_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128365;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6ip6_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128365;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip6ip6_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128366;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip6ip6_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128366;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_check_defrag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128370;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_check_defrag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128370;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_cmsg_recv_offset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128372;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_cmsg_recv_offset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128372;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_cmsg_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128374;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_cmsg_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128374;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_defrag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128376;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_defrag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128376;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_fib_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128379;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_fib_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128379;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_frag_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128381;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_frag_queue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128381;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_frag_reasm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128383;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_frag_reasm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128383;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_get_mcast_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128384;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_get_mcast_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128384;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128385;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128385;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_icmp_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128386;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_icmp_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128386;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_local_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128388;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_local_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128388;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_mcast_join_leave(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128389;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_mcast_join_leave(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128389;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_proc_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_proc_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_proc_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128392;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_proc_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128392;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_ra_control(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128394;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_ra_control(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128394;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_recv_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128396;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_recv_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128396;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_rt_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128398;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_rt_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128398;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_set_mcast_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128399;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_set_mcast_msfilter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128399;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128400;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128400;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_sock_set_freebind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128401;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_sock_set_freebind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128401;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_sock_set_mtu_discover(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_sock_set_mtu_discover(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_sock_set_pktinfo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128403;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_sock_set_pktinfo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128403;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_sock_set_recverr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128404;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_sock_set_recverr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128404;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_sock_set_tos(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128405;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_sock_set_tos(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128405;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ip_valid_fib_dump_req(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128407;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ip_valid_fib_dump_req(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128407;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipcns_install(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128411;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipcns_install(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128411;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_frags_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128421;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_frags_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128421;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_frags_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128422;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_frags_init_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128422;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_frags_pre_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128423;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_frags_pre_exit_net(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128423;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv4_pktinfo_prepare(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128425;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv4_pktinfo_prepare(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128425;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128426;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128426;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128427;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128427;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128428;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128428;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_hopopt_jumbo_remove(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128429;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_hopopt_jumbo_remove(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128429;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_icmp_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128430;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_icmp_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128430;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_icmp_sysctl_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128431;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_icmp_sysctl_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128431;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_local_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128434;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_local_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128434;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_local_rxpmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128436;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_local_rxpmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128436;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_mc_check_mld(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128437;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_mc_check_mld(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128437;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_mc_check_mld_msg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128438;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_mc_check_mld_msg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128438;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_mc_validate_checksum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128439;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_mc_validate_checksum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128439;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_recv_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128445;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_recv_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128445;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_recv_rxpmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128446;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_recv_rxpmtu(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128446;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_route_seq_setup_walk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128449;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_route_seq_setup_walk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128449;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ipv6_route_yield(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128453;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ipv6_route_yield(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128453;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_is_skb_forwardable(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128599;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_is_skb_forwardable(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128599;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_kernel_getsockname(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128791;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_kernel_getsockname(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128791;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_kernel_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128798;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_kernel_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128798;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_kernel_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_kernel_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_kernel_sendmsg_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128802;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_kernel_sendmsg_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128802;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_kernel_sock_ip_overhead(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128805;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_kernel_sock_ip_overhead(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128805;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_kernel_sock_shutdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128807;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_kernel_sock_shutdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=128807;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_bind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130184;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_bind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130184;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_ca_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130185;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_ca_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130185;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_cancel_work(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130186;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_cancel_work(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130186;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_check_and_set_pending(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130187;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_check_and_set_pending(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130187;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_check_data_fin(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130188;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_check_data_fin(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130188;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_check_listen_stop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130189;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_check_listen_stop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130189;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_check_send_data_fin(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130190;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_check_send_data_fin(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130190;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_check_writeable(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130192;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_check_writeable(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130192;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130193;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130193;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_close_ssk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130195;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_close_ssk(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130195;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_close_wake_up(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130196;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_close_wake_up(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130196;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130197;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130197;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_copy_inaddrs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130199;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_copy_inaddrs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130199;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_copy_ip_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130201;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_copy_ip_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130201;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_data_queue_ofo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130203;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_data_queue_ofo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130203;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_data_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130204;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_data_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130204;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_destroy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130205;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_destroy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130205;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_destroy_common(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130207;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_destroy_common(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130207;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_diag_fill_info(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130209;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_diag_fill_info(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130209;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_disconnect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130210;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_disconnect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130210;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_enter_memory_pressure(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130211;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_enter_memory_pressure(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130211;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_finish_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130212;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_finish_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130212;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_finish_join(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130213;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_finish_join(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130213;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_forward_alloc_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130215;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_forward_alloc_get(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130215;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_get_port(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130216;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_get_port(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130216;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_get_sub_addrs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130218;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_get_sub_addrs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130218;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130222;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130222;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_getsockopt_full_info(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130224;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_getsockopt_full_info(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130224;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_getsockopt_sol_tcp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130226;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_getsockopt_sol_tcp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130226;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_getsockopt_subflow_addrs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130227;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_getsockopt_subflow_addrs(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130227;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_getsockopt_tcpinfo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130228;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_getsockopt_tcpinfo(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130228;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130229;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_hash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130229;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_init_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130230;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_init_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130230;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130231;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_ioctl(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130231;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_ioctl_outq(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130233;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_ioctl_outq(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130233;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_listen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130234;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_listen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130234;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_mib_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130236;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_mib_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130236;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_napi_poll(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130237;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_napi_poll(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130237;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_pending_data_fin(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130239;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_pending_data_fin(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130239;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_poll(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130240;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_poll(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130240;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_put_int_option(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130244;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_put_int_option(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130244;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_rcv_space_adjust(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130248;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_rcv_space_adjust(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130248;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_rcv_space_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130249;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_rcv_space_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130249;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130250;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130250;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_release_cb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130251;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_release_cb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130251;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_reset_tout_timer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130253;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_reset_tout_timer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130253;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_rfree(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130255;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_rfree(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130255;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_schedule_work(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130256;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_schedule_work(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130256;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130257;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130257;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130258;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130258;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_sendmsg_frag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130260;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_sendmsg_frag(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130260;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_set_owner_r(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130262;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_set_owner_r(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130262;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_set_rcvlowat(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130263;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_set_rcvlowat(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130263;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_set_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130264;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_set_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130264;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130265;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130265;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_setsockopt_sol_ip_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130267;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_setsockopt_sol_ip_set(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130267;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_setsockopt_sol_socket(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130268;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_setsockopt_sol_socket(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130268;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_setsockopt_sol_socket_int(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130269;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_setsockopt_sol_socket_int(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130269;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_setsockopt_sol_socket_linger(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130271;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_setsockopt_sol_socket_linger(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130271;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_setsockopt_sol_tcp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130272;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_setsockopt_sol_tcp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130272;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_setsockopt_sol_tcp_congestion(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130273;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_setsockopt_sol_tcp_congestion(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130273;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_setsockopt_v6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130274;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_setsockopt_v6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130274;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_shutdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130275;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_shutdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130275;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_sk_clone_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130277;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_sk_clone_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130277;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_sock_graft(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130278;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_sock_graft(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130278;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_sockopt_sync_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130279;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_sockopt_sync_locked(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130279;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_sol_socket_sync_intval(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_sol_socket_sync_intval(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_stream_accept(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130282;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_stream_accept(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130282;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_subflow_active(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130284;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_subflow_active(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130284;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_subflow_get_retrans(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130285;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_subflow_get_retrans(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130285;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_subflow_get_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130286;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_subflow_get_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130286;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_subflow_process_delegated(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130288;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_subflow_process_delegated(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130288;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_subflow_set_active(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130290;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_subflow_set_active(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130290;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_subflow_shutdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130292;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_subflow_shutdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130292;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_sync_mss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130294;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_sync_mss(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130294;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_token_accept(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130296;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_token_accept(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130296;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_token_destroy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130297;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_token_destroy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130297;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_token_destroy_request(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130298;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_token_destroy_request(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130298;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_token_get_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130302;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_token_get_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130302;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_token_iter_next(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_token_iter_next(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_token_new_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130307;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_token_new_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130307;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_token_new_request(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130309;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_token_new_request(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130309;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_try_coalesce(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130312;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_try_coalesce(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130312;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_unhash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130313;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_unhash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130313;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_mptcp_update_data_checksum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130315;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_mptcp_update_data_checksum(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130315;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_net_rps_send_ipi(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_net_rps_send_ipi(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netdev_get_xmit_slave(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130588;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netdev_get_xmit_slave(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130588;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netdev_rx_queue_set_rps_mask(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130661;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netdev_rx_queue_set_rps_mask(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130661;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netdev_sk_get_lowest_dev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130671;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netdev_sk_get_lowest_dev(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130671;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netif_receive_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130713;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netif_receive_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130713;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netif_receive_skb_core(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130714;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netif_receive_skb_core(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130714;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netif_skb_features(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netif_skb_features(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlbl_calipso_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130749;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlbl_calipso_add(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130749;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlbl_calipso_list(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130751;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlbl_calipso_list(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130751;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlbl_calipso_listall(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130752;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlbl_calipso_listall(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130752;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlbl_calipso_remove(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130757;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlbl_calipso_remove(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130757;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlbl_skbuff_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130793;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlbl_skbuff_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130793;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlbl_skbuff_getattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130794;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlbl_skbuff_getattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130794;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlbl_skbuff_setattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130796;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlbl_skbuff_setattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130796;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlbl_sock_delattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130797;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlbl_sock_delattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130797;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlbl_sock_getattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130798;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlbl_sock_getattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130798;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlbl_sock_setattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlbl_sock_setattr(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130800;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlink_attachskb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130812;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlink_attachskb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130812;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlink_detachskb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130828;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlink_detachskb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130828;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlink_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130837;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlink_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130837;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlink_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130853;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlink_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130853;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlink_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130855;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlink_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130855;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlink_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130859;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlink_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130859;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlink_sendskb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130860;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlink_sendskb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130860;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlink_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130868;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlink_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130868;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlink_skb_destructor(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130869;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlink_skb_destructor(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130869;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlink_skb_set_owner_r(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130870;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlink_skb_set_owner_r(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130870;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlink_sock_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130871;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlink_sock_destruct(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130871;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlink_to_full_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130877;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlink_to_full_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130877;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlink_update_socket_mc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130887;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlink_update_socket_mc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130887;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netlink_update_subscriptions(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130889;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netlink_update_subscriptions(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130889;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netpoll_send_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130900;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netpoll_send_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130900;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netpoll_send_udp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130902;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netpoll_send_udp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130902;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_netpoll_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130906;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_netpoll_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130906;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_nf_ct_get_tuple_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130927;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_nf_ct_get_tuple_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130927;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_nf_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130930;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_nf_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130930;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_nf_ip6_reroute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130945;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_nf_ip6_reroute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130945;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_nf_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130955;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_nf_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130955;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_nf_sockopt_find(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130957;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_nf_sockopt_find(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=130957;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_pad_compress_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=131179;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_pad_compress_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=131179;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_perf_trace_mptcp_subflow_get_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=132159;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_perf_trace_mptcp_subflow_get_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=132159;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ppp_send_frame(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=132716;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ppp_send_frame(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=132716;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ppp_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=132720;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ppp_start_xmit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=132720;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_ppp_xmit_process(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=132731;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_ppp_xmit_process(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=132731;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_secure_tcp_ts_off(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134343;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_secure_tcp_ts_off(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134343;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_secure_tcpv6_ts_off(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134347;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_secure_tcpv6_ts_off(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134347;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_inet_csk_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134453;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_inet_csk_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134453;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_inet_sys_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134455;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_inet_sys_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134455;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_ip_forward(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_ip_forward(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134494;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_ip_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134495;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_ip_output(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134495;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_ip_postroute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134496;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_ip_postroute(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134496;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_ip_postroute_compat(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134498;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_ip_postroute_compat(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134498;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_mptcp_add_subflow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134522;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_mptcp_add_subflow(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134522;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_netlink_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134530;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_netlink_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134530;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_parse_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134535;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_parse_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134535;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_parse_skb_ipv4(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_parse_skb_ipv4(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134536;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_parse_skb_ipv6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134537;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_parse_skb_ipv6(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134537;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_sctp_sk_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134563;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_sctp_sk_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134563;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_sk_alloc_security(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134582;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_sk_alloc_security(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134582;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_sk_clone_security(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134583;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_sk_clone_security(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134583;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_sk_free_security(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134584;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_sk_free_security(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134584;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_sk_getsecid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134585;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_sk_getsecid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134585;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_skb_peerlbl_sid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134587;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_skb_peerlbl_sid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134587;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_sock_graft(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134588;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_sock_graft(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134588;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_sock_rcv_skb_compat(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134590;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_sock_rcv_skb_compat(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134590;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_socket_accept(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134591;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_socket_accept(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134591;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_socket_bind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134592;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_socket_bind(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134592;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_socket_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134593;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_socket_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134593;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_socket_getpeername(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134595;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_socket_getpeername(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134595;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_socket_getpeersec_dgram(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134596;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_socket_getpeersec_dgram(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134596;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_socket_getpeersec_stream(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134597;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_socket_getpeersec_stream(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134597;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_socket_getsockname(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134598;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_socket_getsockname(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134598;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_socket_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134599;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_socket_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134599;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_socket_listen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134600;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_socket_listen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134600;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_socket_post_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134601;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_socket_post_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134601;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_socket_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134602;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_socket_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134602;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_socket_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134603;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_socket_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134603;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_socket_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134604;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_socket_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134604;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_socket_shutdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134605;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_socket_shutdown(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134605;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_socket_sock_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134606;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_socket_sock_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134606;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_socket_socketpair(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134607;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_socket_socketpair(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134607;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_socket_unix_may_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134608;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_socket_unix_may_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134608;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_socket_unix_stream_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134609;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_socket_unix_stream_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134609;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_xfrm_skb_sid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134651;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_xfrm_skb_sid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134651;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_xfrm_skb_sid_ingress(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134652;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_xfrm_skb_sid_ingress(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134652;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_selinux_xfrm_sock_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134654;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_selinux_xfrm_sock_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=134654;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sit_ip6ip6_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sit_ip6ip6_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_msg_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135330;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_msg_alloc(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135330;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_msg_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135332;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_msg_clone(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135332;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_msg_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135334;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_msg_free(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135334;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_msg_free_elem(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135335;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_msg_free_elem(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135335;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_msg_free_nocharge(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135336;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_msg_free_nocharge(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135336;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_msg_free_partial(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_msg_free_partial(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_msg_free_partial_nocharge(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135339;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_msg_free_partial_nocharge(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135339;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_msg_is_readable(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135340;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_msg_is_readable(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135340;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_msg_memcopy_from_iter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135342;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_msg_memcopy_from_iter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135342;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_msg_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135344;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_msg_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135344;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_msg_return(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135346;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_msg_return(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135346;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_msg_return_zero(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135347;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_msg_return_zero(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135347;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_msg_trim(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135349;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_msg_trim(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135349;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_msg_zerocopy_from_iter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135350;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_msg_zerocopy_from_iter(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135350;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_drop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135353;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_drop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135353;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135355;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135355;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_init_strp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135357;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_init_strp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135357;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_link_pop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135359;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_link_pop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135359;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_msg_verdict(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135361;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_msg_verdict(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135361;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_skb_ingress_enqueue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135363;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_skb_ingress_enqueue(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135363;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_skb_ingress_self(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135365;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_skb_ingress_self(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135365;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_skb_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135367;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_skb_redirect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135367;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_start_strp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135368;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_start_strp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135368;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_start_verdict(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135369;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_start_verdict(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135369;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_stop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135371;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_stop(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135371;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_stop_strp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135372;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_stop_strp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135372;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_stop_verdict(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135373;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_stop_verdict(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135373;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_strp_data_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135374;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_strp_data_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135374;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_strp_parse(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135375;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_strp_parse(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135375;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_strp_read(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135376;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_strp_read(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135376;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_strp_read_done(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135377;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_strp_read_done(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135377;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_tls_strp_read(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135379;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_tls_strp_read(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135379;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_verdict_apply(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135381;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_verdict_apply(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135381;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_verdict_data_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135382;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_verdict_data_ready(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135382;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_verdict_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135383;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_verdict_recv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135383;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_psock_write_space(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135384;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_psock_write_space(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135384;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_stream_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135386;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_stream_error(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135386;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_stream_kill_queues(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_stream_kill_queues(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135387;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_stream_wait_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135388;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_stream_wait_close(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135388;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_stream_wait_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135390;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_stream_wait_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135390;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_stream_wait_memory(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_stream_wait_memory(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135391;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sk_stream_write_space(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135392;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sk_stream_write_space(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135392;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_checksum_help(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135393;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_checksum_help(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135393;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_clone_tx_timestamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135394;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_clone_tx_timestamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135394;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_crc32c_csum_help(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135395;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_crc32c_csum_help(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135395;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_csum_hwoffload_help(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135397;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_csum_hwoffload_help(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135397;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_defer_rx_timestamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135398;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_defer_rx_timestamp(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135398;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_network_protocol(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135400;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_network_protocol(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135400;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_udp_tunnel_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_udp_tunnel_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135402;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_skb_warn_bad_offload(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135403;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_skb_warn_bad_offload(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135403;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_alloc_file(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135517;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_alloc_file(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135517;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135521;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_create(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135521;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_create_kern(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135523;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_create_kern(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135523;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_create_lite(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135524;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_create_lite(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135524;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_has_perm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135531;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_has_perm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135531;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_release(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135543;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_release(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135543;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sock_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135545;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sock_sendmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135545;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sockfs_init_fs_context(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135560;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sockfs_init_fs_context(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=135560;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_sync_socket_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136025;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_sync_socket_options(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136025;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_taskstats_user_cmd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136186;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_taskstats_user_cmd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136186;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp4_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136218;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp4_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136218;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp4_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136219;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp4_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136219;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp4_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136220;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp4_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136220;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp6_proc_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136221;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp6_proc_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136221;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp6_proc_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136222;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp6_proc_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136222;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_assign_congestion_control(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136224;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_assign_congestion_control(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136224;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_clamp_probe0_to_user_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136235;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_clamp_probe0_to_user_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136235;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_cleanup_congestion_control(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136236;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_cleanup_congestion_control(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136236;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_cong_avoid_ai(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136239;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_cong_avoid_ai(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136239;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_delack_timer_handler(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136242;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_delack_timer_handler(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136242;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_fastopen_active_detect_blackhole(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136244;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_fastopen_active_detect_blackhole(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136244;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_fastopen_active_disable(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136245;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_fastopen_active_disable(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136245;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_fastopen_active_disable_ofo_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136246;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_fastopen_active_disable_ofo_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136246;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_fastopen_active_should_disable(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136247;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_fastopen_active_should_disable(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136247;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_fastopen_add_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136248;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_fastopen_add_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136248;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_fastopen_cookie_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136250;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_fastopen_cookie_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136250;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_fastopen_create_child(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136252;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_fastopen_create_child(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136252;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_fastopen_ctx_destroy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136253;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_fastopen_ctx_destroy(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136253;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_fastopen_defer_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136256;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_fastopen_defer_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136256;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_fastopen_destroy_cipher(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136257;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_fastopen_destroy_cipher(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136257;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_fastopen_get_cipher(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136259;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_fastopen_get_cipher(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136259;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_fastopen_init_key_once(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136260;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_fastopen_init_key_once(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136260;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_fastopen_reset_cipher(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136262;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_fastopen_reset_cipher(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136262;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_get_cookie_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136266;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_get_cookie_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136266;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_get_default_congestion_control(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136268;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_get_default_congestion_control(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136268;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136269;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136269;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136270;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136270;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136271;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_gso_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136271;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_init_congestion_control(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136272;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_init_congestion_control(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136272;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_init_xmit_timers(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136273;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_init_xmit_timers(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136273;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_out_of_resources(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136276;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_out_of_resources(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136276;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_probe_timer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136277;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_probe_timer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136277;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_reno_cong_avoid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136280;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_reno_cong_avoid(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136280;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_reno_ssthresh(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_reno_ssthresh(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136281;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_reno_undo_cwnd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136282;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_reno_undo_cwnd(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136282;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_retransmit_timer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136283;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_retransmit_timer(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136283;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_set_ca_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136286;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_set_ca_state(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136286;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_set_congestion_control(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136288;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_set_congestion_control(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136288;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_set_default_congestion_control(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136290;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_set_default_congestion_control(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136290;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_set_keepalive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136291;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_set_keepalive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136291;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_slow_start(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136293;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_slow_start(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136293;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_syn_ack_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136295;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_syn_ack_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136295;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_try_fastopen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136297;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_try_fastopen(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136297;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_conn_request(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136302;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_conn_request(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136302;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136303;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136303;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_do_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136304;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_do_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136304;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_early_demux(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_early_demux(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136305;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136306;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136306;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_fill_cb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136308;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_fill_cb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136308;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_get_syncookie(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136310;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_get_syncookie(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136310;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_init_seq(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136311;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_init_seq(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136311;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_init_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136312;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_init_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136312;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_init_ts_off(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136313;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_init_ts_off(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136313;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_md5_hash_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136314;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_md5_hash_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136314;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_md5_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136315;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_md5_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136315;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_mtu_reduced(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136316;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_mtu_reduced(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136316;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_parse_md5_keys(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136317;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_parse_md5_keys(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136317;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_pre_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_pre_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136318;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136319;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136319;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_reqsk_destructor(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136320;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_reqsk_destructor(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136320;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_reqsk_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136321;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_reqsk_send_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136321;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_restore_cb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136322;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_restore_cb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136322;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_route_req(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136323;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_route_req(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136323;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_send_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136324;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_send_check(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136324;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_send_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136325;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_send_reset(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136325;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_send_response(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136327;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_send_response(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136327;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_send_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136328;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_send_synack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136328;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_syn_recv_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136329;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_syn_recv_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136329;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_v6_timewait_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136330;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_v6_timewait_ack(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136330;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_write_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136332;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_write_timeout(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136332;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcp_write_timer_handler(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136334;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcp_write_timer_handler(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136334;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcpv6_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcpv6_net_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136338;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tcpv6_net_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136339;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tcpv6_net_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136339;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_thermal_genl_cmd_tz_get_trip(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136444;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_thermal_genl_cmd_tz_get_trip(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136444;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_thermal_genl_event_tz_trip_change(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136454;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_thermal_genl_event_tz_trip_change(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136454;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_thermal_genl_event_tz_trip_up(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136455;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_thermal_genl_event_tz_trip_up(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136455;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_thermal_genl_send_event(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136459;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_thermal_genl_send_event(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136459;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tomoyo_socket_bind_permission(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136734;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tomoyo_socket_bind_permission(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136734;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tomoyo_socket_connect_permission(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tomoyo_socket_connect_permission(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136735;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tomoyo_socket_listen_permission(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136736;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tomoyo_socket_listen_permission(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136736;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_tomoyo_socket_sendmsg_permission(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136737;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_tomoyo_socket_sendmsg_permission(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=136737;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_trace_event_raw_event_mptcp_subflow_get_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137034;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_trace_event_raw_event_mptcp_subflow_get_send(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137034;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp4_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137572;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp4_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137572;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp4_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137573;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp4_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137573;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp4_ufo_fragment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137574;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp4_ufo_fragment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137574;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp6_ehashfn(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137575;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp6_ehashfn(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137575;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp6_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137576;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp6_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137576;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp6_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137577;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp6_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137577;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp6_lib_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137579;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp6_lib_lookup(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137579;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp6_lib_lookup2(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137581;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp6_lib_lookup2(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137581;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp6_lib_lookup_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137582;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp6_lib_lookup_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137582;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp6_proc_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137583;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp6_proc_exit(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137583;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp6_proc_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137584;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp6_proc_init(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137584;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp6_ufo_fragment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137586;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp6_ufo_fragment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137586;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp6_unicast_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137587;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp6_unicast_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137587;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137589;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_gro_complete(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137589;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137591;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_gro_receive(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137591;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_gro_receive_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137592;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_gro_receive_segment(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137592;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_v6_early_demux(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137593;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_v6_early_demux(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137593;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_v6_get_port(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137594;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_v6_get_port(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137594;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_v6_push_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137595;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_v6_push_pending_frames(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137595;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_v6_rehash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137596;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_v6_rehash(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137596;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udp_v6_send_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137598;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udp_v6_send_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137598;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udpv6_destroy_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137600;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udpv6_destroy_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137600;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udpv6_destruct_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137601;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udpv6_destruct_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137601;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udpv6_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137603;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udpv6_err(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137603;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udpv6_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137605;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udpv6_getsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137605;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udpv6_init_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137607;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udpv6_init_sock(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137607;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udpv6_pre_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137610;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udpv6_pre_connect(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137610;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udpv6_queue_rcv_one_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137611;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udpv6_queue_rcv_one_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137611;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udpv6_queue_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137612;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udpv6_queue_rcv_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137612;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udpv6_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137613;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udpv6_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137613;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udpv6_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137614;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udpv6_recvmsg(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137614;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udpv6_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137616;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udpv6_setsockopt(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137616;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_udpv6_splice_eof(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137617;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_udpv6_splice_eof(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=137617;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_validate_xmit_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=138153;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_validate_xmit_skb(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=138153;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_validate_xmit_skb_list(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=138154;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_validate_xmit_skb_list(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=138154;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_validate_xmit_xfrm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=138156;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_validate_xmit_xfrm(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=138156;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xdp_build_skb_from_frame(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=139013;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xdp_build_skb_from_frame(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=139013;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xfrm6_gro_udp_encap_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=139322;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xfrm6_gro_udp_encap_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=139322;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}


int ktprobe_xfrm6_udp_encap_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=139334;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=0;
    events.ringbuf_submit(data, 0);
    return 0;
}
int ktretprobe_xfrm6_udp_encap_rcv(struct pt_regs *ctx)
{
    struct SkProbe *data = events.ringbuf_reserve(sizeof(struct SkProbe));
    if(!data){return 0;}
    data->FuncID=139334;
    data->kernelTime = bpf_ktime_get_ns();
    data->pid=bpf_get_current_pid_tgid();
    data->ret=1;
    events.ringbuf_submit(data, 0);
    return 0;
}

